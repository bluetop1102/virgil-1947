# Claude Code

@AGENTS.md

## Claude 전용

- 웹 확인은 gstack `/browse` 스킬 또는 `npm run shot` 하네스를 쓴다. `mcp__claude-in-chrome__*`는 쓰지 않는다.
- 시각 검수는 반드시 `npm run shot`으로 PNG를 만든 뒤 Read 도구로 이미지를 직접 본다. 코드만 읽고 품질을 판정하지 않는다.
