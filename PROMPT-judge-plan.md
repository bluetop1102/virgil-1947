# 발사문 — JUDGE 평가 + 개선 계획 (Fable 단독 세션)

> 실행자: Fable 새 세션. **평가와 계획만 한다 — 코드 수정·커밋·발주 금지.**
> 판정 대상은 **배포본** `https://bluetop1102.github.io/virgil-1947/`.
> 제출 **8/10 밤**. 개선 창은 **오늘(8/9) 16:00 → 24:00 의 8시간**.

## 0. 이 세션이 왜 필요한가

네가 두 번 판정했고(`docs/reviews/fable-experience-2026-08-08.md` · `-08-09.md`),
2차 결론은 **실격 0 · G 평균 6.6 · 8+ 항목 0**이었다. 흠은 사라졌는데 뛰어난 항목이 없다.

사용자 판단은 이렇다 — **"처음 플레이해보는 심사관/유저가 '와 쩐다' 느낄 수준이 아니다."**
맞는 진단이고, 원인은 구현이 아니라 **루브릭**에 있다. `AAA-RUBRIC.md`는 결함 제거 루브릭이라
모든 값을 6~7로 수렴시키는 것이 그 루브릭을 성실히 따른 결과다.

그래서 직교 루브릭을 새로 만들었다: **`docs/JUDGE-RUBRIC.md`. 이 세션의 채점기는 그것이다.**
평균이 아니라 피크를 보고, 항목이 아니라 순간을 센다.

## 1. 먼저 읽는다

1. **`docs/JUDGE-RUBRIC.md`** — 이 세션의 채점기. J1~J6 축과 채점 규칙(피크 조건·바닥 조건).
2. `docs/reviews/fable-experience-2026-08-09.md` — 네 2차 판정. **재발견에 시간 쓰지 마라.**
   특히 §0-6(심문 푸시인 램프 오클루전)은 이미 확정된 단일 원인이다.
3. `docs/RESUME.md` §3 — 기각된 가설 17건.
4. `AGENTS.md` — 소유권·커밋 규약·**에셋 예외 2건**(타이틀 배경 AI 이미지 · BGM 외부/AI 허용).

## 2. 사용자가 명시한 우선순위

판정에 반영하되 네 관측으로 검증해라 — 사용자 말을 그대로 받아쓰지 마라.

1. **BGM이 매우 중요하다.** "공포 추리 게임이니까." → J6.
   외부 라이선스 에셋·AI 생성 트랙 사용이 승인됐다(AGENTS.md 예외 ②).
   **다만 sfx·룸톤·리버브는 절차 생성을 유지한다.**
2. **UI/UX는 AAA 게임이 당연히 갖춘 것들이 있어야 한다.** → J5.
   N8이 이미 9점인데 그건 "예쁘다"의 증명이다. **여기서 볼 것은 기능**이다 —
   처음 켠 사람이 조작을 어디서 배우는가, 지금 목표를 아는가, 상호작용 가능한 것이 구분되는가,
   일시정지가 되는가, 설정에서 음량·감도·자막을 바꿀 수 있는가.
3. **캐릭터·대화 시 표정.** → J3. 램프 오클루전이 여기 직결된다.

## 3. 작업

### 3-1. J1~J6 채점

`docs/JUDGE-RUBRIC.md` §3 산출 형식대로. **①현재 점수 ②이유(프레임·로그 좌표)
③9점이 되려면 ④8시간 창 안에 가능한가 ⑤소유 파일·예상 소요.**

- 배포본을 **처음 켜는 사람의 동선**으로 걸어라. 네가 아는 조작을 쓰지 말고,
  "안내 없이 시작한 사람이 무엇을 할 수 있는가"를 먼저 재라(J5의 핵심).
- 점수는 **증거로 뒷받침**한다. 인상 서술만으로 매기지 마라.
- 오디오는 헤드리스라 청감 불가다 — J6은 **코드·이벤트 구조와 무음 구간의 길이**로 재고,
  사람이 들어야 할 항목은 분리해 명시해라.

### 3-2. 개선 계획 — 병렬 4~5세션 분배안

이게 이 세션의 실질 산출이다. 다음을 지켜라.

- **파일 소유가 겹치지 않게 갈라라.** 같은 워킹트리에서 동시에 돌기 때문이다.
  기존 분할선 참고: S1 `world/lobby*·materials·props` · S2 `render/**·atmosphere·atmo` ·
  S3 `narrative/**·ui/**·chars/**·gameplay/**` · S4 `tools/**` · S5 `audio/**` · S6 `ui/title.js`.
  이 선을 그대로 쓸 필요는 없지만 **겹치면 안 된다.**
- 각 세션에 **무엇을 · 어느 파일에서 · 무엇으로 검증하는지**를 붙여라. 발주문 초안까지 쓰면 좋다.
- **의존성을 표시해라** — 먼저 끝나야 하는 것이 있으면 순서를 명시.
- **포기 목록을 반드시 쓴다.** 8시간에 안 들어오는 것을 조용히 빼지 말고 "포기: 이유"로 남겨라.

### 3-3. 판단이 필요한 곳

- **BGM을 외부/AI 에셋으로 갈지, 절차 생성을 더 밀지.** 현재 1막 드론이 있다.
  에셋을 쓰면 라이선스·출처 기재 부담과 톤 통제 상실이 따르고, 절차 생성은 시간이 든다.
  **네 권고를 근거와 함께 내라** — 최종 결정은 사용자가 한다.
- **J5에서 무엇을 추가하면 "AAA가 당연히 갖춘 것"이 서는가.** 디제틱 원칙(D7 실격 회피)과
  충돌하지 않는 선이어야 한다. 시스템 폰트·기본 버튼·이모지는 여전히 금지다.

## 4. 금지

- **코드 수정·커밋·발주 금지.** 판정문과 계획서만 쓴다.
- 기계 게이트 재실행 금지 — 2차 판정의 실측을 인용해라.
- 이미 확정된 것을 재발견하지 마라(램프 오클루전·수용된 미달 5건).
- 실제 사건 피해자 재현 금지. 인물·사건은 전부 허구.

## 5. 산출

`docs/reviews/judge-plan-2026-08-09.md` 하나. 프레임 증거는 `docs/reviews/shots-judge/`.

마지막에 사용자에게 **한 문단**으로 보고한다 — 지금 이 게임이 심사관에게 어떻게 읽힐지,
8시간을 어디에 쓰면 그 인상이 가장 크게 바뀌는지. 점수표 나열이 아니라 **판단**을 써라.
