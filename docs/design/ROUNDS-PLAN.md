# ROUNDS-PLAN — 기획 라운드 로그

> 형식: 라운드마다 `증상 → 원인(실측) → 수정 → 판정`. 채점 원본은 세션 스크래치에,
> 점수와 실패 좌표는 여기 남긴다. 커밋은 라운드 단위(디렉터 전용).

## R-A (Phase A) — 채점기 고정 · 2026-08-05

- **산출**: `graders/grader-M.md` · `graders/fresh-test.md` · `case-graph.json` v2 ·
  `tools/factcheck.mjs` · `E0-index.md`(분해 확정·명칭 검증).
- **판정**:
  - factcheck: S0·F1~F4·R1~R3 **전 게이트 PASS** (거짓 진술 7건 반박 경로 성립 ·
    증거 14종 원산지 셀 결박 100% · 최악 소각 경로 미제 엔딩 도달 · 고아 0 · LORE 격리).
  - 변이 테스트 5종(F2 위치 모순·F1/F3 획득 경로 제거·F4 고아 주입·R3 원산지 변조·
    F3 진행 불능 주입) **전부 검출** — 죽은 게이트 아님 확인.
  - 명칭 검증: "The Virgil"은 LA 실존 바·공연장(숙박업 아님), "Hotel Virgil" 실존 호텔·
    상표 미확인 → **버질 확정**(외부 공개 시 USPTO 재확인 조항).
- **비고**: case-graph 데이터는 MASTER-PLAN §3.2 확정 매트릭스의 데이터화라 Phase A에서
  선행 작성(E3 라운드는 지휘 문서 작성 + 블라인드 검증에 집중). 결함 3건 수정은 R1~R3로
  기계 결박: 유인/추적 미결은 **유인**으로 확정(FA4 — 계획성·동기 정합).
- **커밋**: `b41596c`

## R-B (Phase B) — E1·E2 승부처 · 2026-08-05

- **게이트 1 경위**: 최초 3안(물의 증언/소각의 형사/괴담의 공범)을 내부 렌즈 차이로 제시 →
  사용자 반려("세그먼트가 기존에 무엇을 즐기고 우리가 무슨 가치제안을 하는가에서 도출하라").
  세그먼트 지도 재작성(S1 유레카파·S2 심문 드라마파·S3 대기 공포파·S4 AI 자유입력파 —
  근거: PC Gamer 2025 결산, Smoking Gun 리뷰 실측) → 가치제안 3안 재제시 →
  **안 A "비가역 대면"(S2×S1) 확정**. 파생 요구 2건 접수: 병목 배분·손실의 서사적 인정
  (→ E3 §3·E5 §2로 반영).
- **산출**: E1(가치제안 결박·U1~U5·클러치 C1~C5) · E2(50분 타임라인·첫 30초 초 단위·
  실패 루트 부록) · E0 정렬표 착수. 커밋 `a4f9c51`.
- **판정**: 블라인드 채점(grader-RB, E1·E2 스코프) 백그라운드 — 결과 대기 중.

## R-C1~C8 (Phase C) — 원소 직렬 집필 · 2026-08-05

| 라운드 | 산출 | 커밋 |
|---|---|---|
| R-C1 | E3 지휘 문서 + STORY v2(재허구화·결함 3건 대사 정합·괴담 §7 신설·미제 자막) — grep 세실 0·factcheck PASS | `011a40d` |
| R-C2 | E4 페르소나 4장(4축+아크) + 도일 프리젠스 신설 + rig/perf 계약 | `d4f120d` |
| R-C3 | E5 판정표 진실원 이관(STORY §4→포인터)·소각 경제·막 전환·2단 커널 경계 | `9d00fd5` |
| R-C4 | E6 공간 5권역 조립 사양·소유권 4모듈 분할(944=room942.js)·그래픽 동결 조항 | `602ba5a` |
| R-C5 | E7 카메라 수치 사양·리버브 표·물 리트모티프·괴담 매체 4종 | `9a4b487` |
| R-C6 | E8 노트 3확장(스크럽·소각 잔존·괴담 지면)·수동 저장 배제(U1 지지대) | `ae3829c` |
| R-C7 | E9 게이트 체계 + AAA-RUBRIC v2(P축·게이트 4·순서 4→2→1→3) + ARCH v2(§0·§2·§5) | `7e8834a` |
| R-C8 | E10 티켓 보드(P0 5장·P1 7장·의존 그래프) + E0 정렬표 전 요소 | `e175ba6` |

설계 결정 기록: 막 전환 증거 요구 0(진행 불능 원천 차단) · 수동 저장 배제(소각 회피 봉쇄) ·
944호는 room942.js 소유 · 보일러실은 공간이 아니라 소리 원점 · 넬의 옥상행은 **유인**으로
확정(계획 살인 정합).

## R-D (Phase D) — 통합 검수

### R-D0 측정 — 신선 테스트 6/10 + 블라인드 채점(E1·E2)

- 신선 테스트: E1~E6 완료(차단성 질문 13건 · assumptions 49건), E7~E10은 **세션 한도로
  에이전트 실패**(15:50 KST 리셋 후 캐시 재개 예정 — runId `wf_ba5a1ba1-7b8`).
- 블라인드 채점(grader-RB, E1·E2 스코프 — Phase C 완성 전 시점 채점): M2 3 · M3 0 ·
  M4 6 · M5 4. 지적 15건 중 4건은 낡은 좌표(E5·E9 부재, 정렬표 공란, STORY 자막 —
  R-C 라운드가 이미 해소), 11건 유효.

### R-D1 수정 — 원인 실측 기반 (증상→원인→수정→판정)

- **증상**: 차단성 질문 13건(discount 0 — 전건 문서에 답 부재 확인). 원인 분포: MISSING 9 ·
  AMBIGUOUS 2 · CONFLICT 2 — **정보 부재(계약 공백)가 지배적**, 서술 모호가 아님.
  공통 구조: "크로스 소유자 접점의 미계약"(이벤트 채널·UI 소유·봇 구동·충돌 규약).
- **수정(설계 결정 6건 + 계약 등재)**:
  1. 심문 종료 3단 = 상태식(소각 0∧전 거짓 반박 / key 소각≥1 / 그 외) + 노트 요약 문구
     9건 STORY 집필 + 재판정 점수 대체 규칙 (E5 §2.4).
  2. DOUBT 재질문 = 진위 불문 1회(TRUTH/LIE만) + 점수 대체 규칙 — 판별기화·공짜 탐침
     양쪽 봉쇄 (E5 §1).
  3. 심문 UI 소유 = [UI] (hud 3선택 + notebook 지목 모드) — `interrogation:prompt`/`choose`
     이벤트 신설, T-P1-09 티켓 신설.
  4. 심문 카메라·침묵 연출 = T-P1-08 신설(cinematics P1 조기화) — P1 게이트의 P4 소유자
     충돌 해소.
  5. perf 상태 채널 = `perf:state` 신설 + `anxiousTell` 필드(case-graph, 4건) + factcheck
     P5d 정적 검사(36% PASS) + `deduction:link` 신설.
  6. 완주 봇 구동 = `__CECIL__.qa` API(ARCH §9, 슬러그=obtain.where, fast/paced 2모드) ·
     room 정본 어휘 등재 · 레벨 충돌=physics 정적 등록 규약 · script 소비자 적응=T-P0-03
     범위 확장(case-graph-loader 신설 허용).
  - R-B 채점 유효 지적 반영: E1 [플레이어]↔[구현] 정합 4건 · E2 무사건 2곳 분해(32:00
    lore 분리·33:30 층계창 관찰 신설 / 엔딩 시네마틱 비트 분해+무사건 제외 규칙) · 사건
    t=구간 시작 단일값 규칙 · roofkey 필적 산출 결박(FB3) · 부록 B→게이트 판정 캡처 목록 ·
    U1 소각 경제 심화("그래프를 태운다" — M5 동형성 절단 논거).
- **판정**: factcheck 전건 PASS(P5d 포함 10항목). 재채점은 E7~E10 신선 테스트 재개와 함께
  전 항목 통합 채점으로. 커밋 `02f2dd2`.

### R-D2 재측정 — 수정 후 신선 테스트 (7/10)

- **결과**: E1 3→1 · E2 1→3 · E3 1→1 · E4 3→**0** · E5 3→2 · E6 2→1 · E7 신규 2.
  E8~E10 세션 한도 재실패. 총 차단 13→10건 — 수정된 접점은 닫혔고(E4 완전 해소),
  문서가 정밀해지며 다음 층의 계약 공백이 드러나는 수렴 패턴.
- **판정**: 유효 10건 / discount 0. 공통 구조는 여전히 크로스 소유자 접점 —
  봇 입력·관측 채널, 비증거 qaId 어휘, breaking 산출 규칙, 엔딩 변주 원문,
  v1 분기 대사 지위, DoF 제어 계약, cin-act3 경계, --paced 의미론.

### R-D3 수정 — 10건 일괄 해소 · 커밋 `915d59e`

- qa API 확장(walk·choose·link·state·events — 봇은 버스 직접 접근 금지) + 비증거 qaId
  정본 목록 + --paced 스케줄 의미론(ARCH §9) · perf:state breaking 규칙 =
  `breakingOn` 3건(붕괴는 인물당 한 번 — 프라이스 S4 고해는 anxious 지속) ·
  `camera:dof` 이벤트 신설(pipeline 수신부는 HANDOFF) · footprints `corridor9/footprints`
  슬러그 · **STORY §5.4 플래그 변주 원문 9문장 저작**(플래그 3종 × 엔딩 3종) ·
  v1 분기 대사 = 캐논 유지 선언 + 모순 1건 교체(pryce.S2 onDoubt "46년 8월") ·
  cin-act3 = 보행 중 연출 확정. factcheck 전건 PASS 유지.

### R-D4 측정 — 신선 전수 10/10 성공 (첫 완전 측정)

- **결과**: E3·E5·E9 차단 **0** (M1 10점 확보) · E1 3(신규 심층) · E2 2 · E4 1 · E6 3 ·
  E7 1 · E8 2 · E10 1 — 총 13건. 질문 층위가 "문서 결함"에서 "마지막 미결 설계 결정"으로
  하강(지목 종결 트리거·소각 영속 시점·앵커 계약 등). 유효 13 / discount 0.

### R-D5 수정 — 13건 일괄 해소 · 커밋 `c6b95d1`

- 설계 결정: 지목 종결=**서명 상호작용**(자동 판정은 무힌트 위반, L3만 자동 종결) ·
  소각=**이중 레이어 영속**(비가역 write-through + 막 경계 스냅샷 — E1 코어와 E8의
  모순 해소) · 텔 채널 분리(신체 클립 perf ≥3 / 음성·대사는 STORY 원문 캐논) ·
  앵커 규약(레벨=빈 앵커, chars 자기 배치) · 밀도 차등(944 ≥4) · act:phase 어휘
  (early/main/late) · interrogation:end에 tier · aiming/present/sign/boot:progress 이벤트 ·
  qa.sign/notebook · T-P1-10 신설(gameplay QA 하네스).
- **판정**: 차단 보유 7요소 신선 재테스트(R-D5 판정 런) 실행 — 프로토콜 1건 명확화
  포함("명시적 이연 조항은 답이다"). 라운드 예산 소진점 — 이 측정 후 결과 불문
  통합 채점으로 진행, 잔여는 게이트 2에서 보고.

### R-D5 판정 런 결과 — 차단 13→8 수렴

- E1 1 · E2 1 · E4 1 · E6 2 · E7 1 · E8 1 · E10 1 = 8건. 전부 "이벤트 1개·값 1개·문구
  1건" 수준의 극미세 계약점(deduction:end 채널 · 원경 앵커 · 관찰 문구 조달 · 룸톤 증빙
  채널 · 타이틀 소유 · lore.1912 경유 · 토큰 상한 수치). 유효 8 / discount 0.

### R-D6 수정 — 8건 일괄 해소

- `deduction:end {ending, links, flags}` 이벤트 신설 · 원경 앵커 2종(배치 전용, sighted는
  도일 전용 명문화) · STORY §6 관찰 문구 3건 저작(회색 팻말·채광정 난간·고인 물) + E6
  권역별 상호작용 목록 확정(7/6/6/4/6) · lore.1912 골든 패스 편입(로비 수색 행 —
  lore:heard 4종 봇 도달 성립) · 룸톤 증빙=test-audio 배터리 위임 · 타이틀 `ui/title.js`
  신설(T-P1-05, 부트 순서 로딩→타이틀→intro/복원) · goal 토큰 상한 필수 필드화
  (P0 500만 · P1 1,500만, 발사문 2종 반영).
- **판정 결과 (R-D6 판정 런)**: 차단 8→**4** — E1·E2·E4·E10이 0으로 닫힘. 전체 10요소 중
  7요소 무질문 확정(E1~E5·E9·E10).

### R-D7 수정 — 잔여 4건 해소 · 커밋 `fe6c44a`

- STORY §6 관찰 문구 4건 저작(프런트 압지 — 필적 모티프 선노출·린넨 카트 젖은 시트·
  944 재떨이·커튼 틈, §5.3 지문의 §6 승격) · `spawn/act1~3` 앵커 신설(복원 시작 위치) ·
  막·페이즈 오케스트레이션 소유 = interrogation.js 명시 · 시퀀스 캡처 = 완주 봇
  `--capture` 통합.
- **판정 런 1차(부분)**: E7 **0** 확정 · E8 1건(설정 진입 트리거) · E6·채점기는 모델 한도
  실패 → 재발사.

### R-D8 수정 — E8 설정 진입 트리거 확정 · 커밋 `7bd731b`

- Esc 1회 = 포인터록 해제 + 일시정지 + 설정 서류함 카드(단일 경로, 타이틀 재사용,
  즉시 반영은 카드 열린 상태의 실시간 적용으로 확인).

### 통합 블라인드 채점 1차 (전 요소 E1~E10)

| M1 | M2 | M3 | M4 | M5 | M6 | M7 |
|---|---|---|---|---|---|---|
| 4 | 9 | 6 | **10** | **10** | **10** | 9 |

- **M5 10 (제목 가림)**: "시스템 사양 수준에서 실질 동일한 상용작 없음 — 소각 경제·미제
  정식 엔딩·괴담 격리는 L.A. Noire·역전재판·Her Story·Obra Dinn·Disco Elysium 어디에도
  없다. U1~U5 전건이 파일·절 좌표로 증명, 감점 0."
- 미달 원인은 E8 수렴: M1=E8 신규 질문 2건(일시정지 전파·복원 재개), M3=E8 §2·§5 코어
  무기여(-4). 그 외 M2 -1(942 창광 '흐린 오후' vs 새벽 개시 — 시간 구조 미선언),
  M7 -1(T-P0-03 소유 범위 vs E5 [위임]).

### R-D9 수정 — 채점 미달 좌표 전건 해소 · 커밋 `899c3c1`

- **M1**: `game:pause {on}` 계약(렌더 지속·모듈 자기 스킵·core 무수정·시네마틱 재생 정지) ·
  "이어서" 복원 = `?resume=1` 리로드 프로토콜(부트 경로 재사용 — 신규 전파 이벤트 원천
  불요, `?q=` 감사 전례 준용).
- **M3**: E8 §5 계측을 E9 프레임 예산 행으로 이관 · §2에 코어 기여 결박(Esc 통합 =
  심문 중 오조작 차단 = U1 공정성 조건) + E0 정렬표 E8 행 확장.
- **M2**: 픽션 시간 구조 선언 — 새벽 2시 → 오전 → 오후·비(표 C 승계). 막 경계에서 빛으로
  시간 경과 선언(E2), 942 창광 '흐린 오전'(E6), E1 '하루의 대면'.
- **M7**: E5 [위임]에 T-P0-03 데이터 접근층 편집 승인 조항.

### 통합 블라인드 채점 2차

| M1 | M2 | M3 | M4 | M5 | M6 | M7 |
|---|---|---|---|---|---|---|
| 4 | 7 | **10** | **10** | **10** | **10** | **8** |

- R-D9 수정 전건 인정(M3 6→10, M7 9→8은 신규 좌표 2건 발견 때문). 잔여: M1=E8 신규
  질문 2건(lore 영속 범위 · **브라우저 제스처 게이트** — 자동재생·포인터록 요건과 사양
  충돌이라는 플랫폼 층 지적), M2=E7·E8 [플레이어] 블록의 산출 무체험 3건.

### R-D10 수정 — 2차 채점 미달 좌표 해소 · 커밋 `62e6813`

- **M1(E8)**: lore 접촉 기록 = 비가역 write-through 직렬화(복원 후 괴담 면 재구축) ·
  제스처 게이트 확정 — 무저장 회차 타이틀은 "첫 입력 시 통과"("프런트 벨을 누르십시오"),
  첫 입력이 AudioContext·포인터록을 활성화하고 **그 직후가 E2 0:00 기점**(로딩 중
  선재생 폐기).
- **M2**: E7 [플레이어]에 공간 음향·발소리 체험 문장, E8 [플레이어]에 부트~타이틀 체험.
- **M7**: E8 [위임]에 title.js·hud.js 등재(티켓 원료 완결).

### 통합 블라인드 채점 3차

| M1 | M2 | M3 | M4 | M5 | M6 | M7 |
|---|---|---|---|---|---|---|
| 6 | **9** | **10** | **10** | **10** | **10** | **10** |

- **M1 제외 전 항목 8+ 달성.** 잔여: E8 질문 1건(타이틀→cin-intro 개시 신호) +
  M2 -1(E7 "숨소리" — 사양 없는 소리 약속).

### R-D11 수정 · 커밋 `02156ee`

- `title:proceed {mode}` 이벤트 신설(발주 분리 접면은 이벤트만) · E7 숨소리 약속을
  실제 산출(마른 잔향)로 정합.
- **판정 런**: E8 신규 질문 1건 — **엔딩 후 저장 슬롯의 지위**(서명 종결을 재플레이로
  되돌릴 수 있는가 — U1 해석이 걸린 양갈래).

### R-D12 수정 · 커밋 `a66f7b1`

- **엔딩 = 회차 종결 확정**: save가 deduction:end를 소비해 엔딩을 비가역 레코드에 기록,
  "이어서" 소멸("처음부터"+지난 투숙 각인 한 줄), 크레딧 후 타이틀 복귀 — 서명 비가역의
  저장 층 완결. E8 수명주기(부트→제스처→플레이→일시정지→막 경계 저장→복원→엔딩→종결)
  전 구간 계약 커버.
- **판정**: E8 꼬리 3라운드 추가 발생 — R-D13(재개 부트 재제스처: 리로드가 사용자
  활성화를 소멸, `e3ca89b`) · R-D14(저장 트리거 4건 완결 — 회차 개시 저장 신설로 1막
  중단의 소각 소멸 차단, `b1f5519`) · R-D15(재제스처 재개 신호 = title:proceed wake,
  `379f7be`).

## Phase D 종료 판정 — 게이트 2 진입 (2026-08-06)

- **최종 점수표**: M1 6 · M2 9 · M3 10 · M4 10 · M5 10 · M6 10 · M7 10 (3차 백지 채점 +
  이후 수정은 감점 좌표 제거·계약 추가만).
- **M1 상태**: E8 외 9개 요소 무질문(0건) 확정. E8은 7라운드 연속 "라운드당 정확히 1건"
  패턴 — 전부 U1×저장×브라우저 플랫폼 결합부의 실질 설계 결정이었고 전건 즉시 확정·반영
  (질문 궤적: 진입 트리거→일시정지 전파→복원 프로토콜→lore 영속→제스처 게이트→개시
  신호→엔딩 종결→재제스처→개시 저장→재개 신호). 추가 재테스트 계속 여부는 게이트 2에서
  디렉터 결정 — 예산 조항(라운드 8~14회)을 크게 초과한 상태.
- **누적 실측**: 신선 테스트 총 7랠리·차단성 질문 44건 발굴·전건 해소(discount 0).
  블라인드 채점 3회(백지). factcheck 상시 PASS(10항목). 커밋 45건(라운드 단위).

## 게이트 2 — 사용자 최종 승인 (2026-08-06)

**승인.** E8 잔여 꼬리는 P1 발사문의 CONTRACT_CHANGE_REQUEST 경로로 구현 시점에 디렉터가
답한다. 기획 세트 확정 — 다음 세션 진입로: `PROMPT-build-p0.md`(정지작업) →
`PROMPT-build-p1.md`(수직 슬라이스). 외부 모델(GPT sol·Grok build) 투입은 P0 완료 +
P1 1회전 후 E10 티켓 보드로.

---

# 체험 축 세션 (PROMPT-plan-v2 · 2026-08-06)

> 상위 문서: 게이트 2 통과본(E0~E10). 이 세션은 재작성이 아니라 **비어 있던 체험 축을
> 채점기부터 세운다**. 입력: `docs/research/experience-components.md`(§1 등급표 정본) ·
> `docs/research/codex-verdict.md`(§6 디렉터 표기 = 작업 범위). 병렬 세션(src/·
> tools/calibration/) 진행 중 — PROMPT-build-p0.md 동결, HANDOFF append만.

## R-XA (Phase A) — 체험 축 채점기 신설 · 2026-08-06

- **증상**: 만점 문서 세트(M3~M7 10점)에 주인공·감정 곡선·계약 이해가 없다 — 현행
  루브릭이 체험 축을 판정하지 않아 생긴 공백 (PROMPT-plan-v2 진단 5).
- **원인(실측)**: ①AAA-RUBRIC v2 서문 "판정 불가 항목은 삭제한다" ②grader-M M3 규칙 3
  "코어 무기여 = 감점"이 체험 요소를 능동 배제(E8 M3 -4 처방이 §5 제거였음이 로그 증거)
  ③M2는 양쪽 공백을 못 잡음 ④M1은 "재밌겠는가"를 묻지 않음.
- **수정 (산출)**:
  - `graders/grader-X.md` **신설** — 축 6항목(§6.1 채택 5축 + §6.3 채택 텔 항목):
    X1 수사자 정체성·동기 / X2 자기 도달감(결론 표현권) / X3 중반 전제·행위 변주 /
    X4 50분 감정 곡선 / X5 비가역 계약의 공정한 이해 / X6 비얼굴 텔 가독성·비확정성.
    항목마다 판정 수단 2단 — 기획 판정(신선 에이전트 대리 프로토콜 P-X1~P-X6, 표본
    5/3/3/5/5/3기) + 본 판정(게이트 4 사람 패널). 근거 좌표 전건 부착(§0.5), 충족
    컴포넌트는 축 아님으로 기록(§0 — C2·C5·C7·C10·C11), 지연 반향은 회귀 게이트로
    강등(디렉터 표기 준수).
  - `AAA-RUBRIC.md` v3 — §체험 채점(X-판정자·X1~X6 본 판정 기준·판정 시점 게이트 4) ·
    게이트 4 통과 기준에 X 추가 · 서문 삭제 조항을 "발명 실패 기록 시에만"으로 축소.
  - `graders/grader-M.md` v3 — M3 규칙 3 기여처에 체험 축 추가(상호 배타 재배치, 소비
    증명 조건으로 프록시화 봉쇄) · M2 충분성 검사 **불추가 결정**(공백 포착은 grader-X
    소유 — 무측정 판정 항목 재생산 방지).
  - `E0-index.md` §2 채점기 목록에 grader-X 등재.
- **판정**: 디렉터 게이트 1(축·판정 수단 확정) 상신. E9 §2 추적표 게이트 등재와
  factcheck --echo 확장은 Phase C의 E9 라운드에서 실행(요소 1라운드 규율).
- **게이트 1·2 통과 (2026-08-06)**: 게이트 1 — 6항목 그대로 승인. 게이트 2 — 주인공
  **안 2(넬 사건 종결 담당자 본인)** 선택. 커밋 `de5a5a7`.

## R-X0 — 체험 축 기준선 측정 · 2026-08-06

대리 판정 프로토콜 실측(신선 에이전트 21기 · run `wf_e6430f2e-a87` · 84만 토큰).
X2는 개정 전이라 실행 불가. 환산·감점은 grader-X 규칙의 디렉터 적용(사후 재채점 라운드와
Phase D는 블라인드 채점자가 수행).

| X1 | X2 | X3 | X4 | X5 | X6 |
|---|---|---|---|---|---|
| **0** | **2** | **10** | **0** | **0** | **8** |

- **X1 = 0**: P-X1 5기 전원 3문항 모두 "모른다"로 시작 — 통과 문항 0(환산 2). 5기 전원이
  호출 사유를 **수도·9층 방 문제로 오독**(실종 언급 0 — 첫 30초 표면에 실종이 없다).
  직접 감점: 주인공 진실원 부재·호출 셀 부재·시각 모순(-6) → 0.
- **X2 = 2**: 명제 후보 배열 부재(상한 4) + 링크 성립 시 게임이 `links[].text`를 대신
  말하는 구조(-2). 대리 판정 실행 불가.
- **X3 = 10**: P-X3 3기 전원 마스킹 4장면 구분 성공 + "반복 아님" 판정(환산 10). 직접
  감점 0(2막 내 구조 변주 2건 — 재심문·조건부 진술). **진단 3의 "전제 반복" 우려는 실측
  반증** — codex 3.3 부분 성립 판정과 일치. 콘텐츠 증분 불요, 회귀 감시만.
- **X4 = 0**: 감정 곡선 진실원 부재(항목 0). 단 5기 재구성 곡선이 강하게 수렴(0-5 기대3 →
  5-10 저점 → 20-30 충격·연민4 → 35-40 긴장4-5 → 40-45 정점5 → 45-50 상실 하강) — C-R3
  저작의 실측 재료. 결핍 실측: 강도 2 이하의 진짜 이완 구간이 없다(최저 2가 1회).
- **X5 = 0**: {영구 손실} 예측 0/5(환산 2) — 5기 전원 "화면 근거 없음·추측" 명시. 계약
  전달 사양 부재(-3) → 0. codex §4.1의 공백이 그대로 실측됐다.
- **X6 = 8**: 분류 정확도 12/13·10/13·12/13(전원 <100% — 비확정성 통과) + 가독성 2/3기
  인정(환산 10). 직접 감점: **도일 "웃는다/웃음을 멈추지 않는다" 신호가 텔 채널 분리
  (E4 [구현])의 신체·음성 어느 목록에도 배정되지 않음**(-2) — P-X6 1기도 페이셜 의존을
  지적. 수정 좌표: E4 [구현] 채널 목록.
- **라운드 순서 재편**: X3 통과로 "심문 변주 라운드"는 콘텐츠 증분 없이 종결. 해당
  라운드는 E4 텔 채널 정합(웃음 배정, X6 8→10)으로 대체.

## R-XC1 — 지목판 횡단 개정 (codex-verdict §6.4 · 예외 승인 단일 라운드) · 2026-08-06

- **증상 (사전 채점)**: X2 = 2 — 91쌍 무차별 대입으로 완전 엔딩 도달 가능 + `links[].text`가
  결론을 게임이 대신 말함(표현권이 게임에 있음).
- **수정 (동반 개정 9건 — 라운드 종료 grep 전건 확인)**:
  ① case-graph `links[].claims` 신설 — 링크당 정답 1 + 오답 2 (오답 전건 fact 참조 또는
  misread 표기: FA6·FB6·FA7 재사용 3, 오독 저작 3) ② E5 §5 — 링크 = 증거 2 + 명제 1,
  후보는 전 링크 합집합 9장 셔플(쌍별 목록 차등 금지 — 그것이 힌트), 개정 근거(표현권
  이전) 명문 ③ E5 §6 — 스왑 경계 "명제 선택 → 명제 서술" ④ E8 §1 — 조서 주장란 물성
  (가설 쪽지 9장, 성립 표시는 도일 반응·카메라뿐) ⑤ ARCH §5 — `deduction:present
  {evidence, claim}`·`deduction:link {id?, claim, ok}`(불성립은 id 미포함 — 근접 링크
  노출도 힌트) ⑥ ARCH §9 — `qa.link(evidence, claimId)` ⑦ E2 3막 40:00/42:00/44:00 행
  명제 선택 반영 ⑧ factcheck J1(후보 ≥3·정답 1)·J2(들러리 금지 — fact/misread 구조
  강제)·J3(시도 공간 하한 300 — 최선 819·최악 495) 신설, **변이 테스트 2종 검출 확인**
  ⑨ E10 — T-P2-11(증거판+명제) 신설·T-P1-09 qa.link 시그니처·T-P1-01 심문 샷 배선
  수용 기준(HANDOFF 답신). 보조: STORY §5.4 도일 오답 반응 4줄 저작. E3 §4 2단 커널
  소비 계약 정합.
- **HANDOFF 응답 2건**: ARCH §5 구독 목록 = **(a) 폐집합** 확정(구독 어휘 규칙 신설,
  v2.2) — **chars 티켓 events에 `game:pause` 추가 필요, 매니페스트 재전사는 발주 세션
  몫**(병렬 세션 통지 필요) · interrogation-deitch 샷 = T-P1-01 수용 기준 명시로 확정.
- **판정 (블라인드 재채점)**: **X2 = 10** (P-X2 3기 × 3링크 전건 정답 + 관계 설명 인정 ·
  들러리 0 · 자동 표출 잔존 0 — run `wf_79a08678-5bc`). factcheck 13항목 전건 PASS.
  2 → 10 개선, 판 갱신.

## R-XC2 — 주인공·호출 계기 반영 (진단 1·2 해소, 게이트 2 안 2) · 2026-08-07

- **증상 (사전 채점)**: X1 = 0 — 주인공 진실원 부재·호출 셀 부재·새벽 2시↔아침 모순.
  P-X1 5기 전원이 호출 사유를 수도 문제로 오독.
- **수정**:
  - case-graph: `FA10` 신설(담당 형사의 자살 종결 서명 — actor `detective` 등록,
    autopsy.supports 결박) · `FC0` 신설(프라이스의 새벽 익명 지명 신고 — t `C-call` 신설,
    pryce.S1.hides 결박 — 그의 "인사만 했습니다"가 신고 사실도 가린다) · `FC1` 개정
    (검은 물 = 10일 밤부터, t B-1011로 이동 — 호출 문장은 FC0로 이전).
  - E1 §주인공 신설 — **무명은 의도 선언**(무명 계약 5조: 이름 발화·표기 0 / 신체 노출
    손·배지만 / 톤 STORY §0 / 서명은 잉크 획 물성만 / 회상·독백 금지) + 코어 기여 경로
    (14개월 전 잘못 믿은 TRUTH가 이 사건을 만들었다 — 소각 공포의 개인사화) + 죄책
    차별화(프라이스=알고도 못 막은 자 / 주인공=몰라서 닫은 자).
  - E0 §3 진실원 표에 주인공 행 등재(사고의 구조적 원인이던 지도 공백 해소).
  - STORY §1 호출 경위 재작성 · §5.1 도입 다이치 "…형사님. 또 오셨군요."(신분+전사
    개방) · §5.4 완전 엔딩 재수사 서명 1줄·미제 자막 둘째 줄("종결 조서의 서명은
    14개월 전과 같은 손이었다").
  - E2 첫 30초 — 자막 4줄(넷째 줄 "942호 손님이 이틀째 보이지 않는다" — 용건 전달)·
    0:22 행 다이치 대사.
- **판정 (블라인드 재채점)**: **X1 = 8** (P-X1 v2 5기: 문항② 5/5 "실종 건으로 942호에
  간다" · 문항③ 통과 · 문항①만 공백 — 신분이 표면에 없음. 직접 감점 0/3 — 진실원·
  셀 결박·시각 정합 전건 확인. run `wf_6ed8f258-065`). 0 → 8 개선, 판 갱신.
  factcheck 전건 PASS.
- **미세 반복 (적용·재측정 이월)**: 채점자 fixDirection대로 다이치 첫 대사에 호칭 부여
  "…형사님. 또 오셨군요."(STORY §5.1·E2 0:22 — 무명 계약 ①이 허용하는 호칭 계열).
  문항① 공백을 닫는 수정. **재측정은 세션 한도(리셋 새벽 2시)로 이월** — run
  `wf_48fd0761-7b8` 전기 실패. 리셋 후 P-X1 v3 재실행 예정.
