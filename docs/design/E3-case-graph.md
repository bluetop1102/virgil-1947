# E3 — 사건·사실 그래프 (지휘 문서)

> 데이터의 진실원은 [case-graph.json](case-graph.json)이다 — 타임라인 셀 25개 · 증거 14종 ·
> 진술 14건 · 지목 링크 3개 · 괴담 유닛 4개. 대사 원문은 `docs/STORY.md` v2.
> 이 문서는 그래프의 설계 원칙과 소비 계약만 지휘한다. 검증기: `tools/factcheck.mjs`.

## 1. 구조 — 노드 4종, 엣지 4종

| 노드 | 정의 | 규칙 |
|---|---|---|
| FACT | 명제 문장 — 두 밤의 타임라인 셀(`kind:event`) 또는 정황(`kind:standing`) | 셀에 없는 사건은 게임에 존재하지 않는다 |
| EVIDENCE | 14종. 반드시 event FACT 하나를 원산지로 가진다 | 원산지 셀 결박 100% (F2) |
| STATEMENT | 심문 진술 — 진실/거짓 | 거짓엔 `hides`(가리는 명제)와 `refutedBy`(반박 증거) 필수 (F1) |
| LORE | 괴담 유닛 — **검증 제외 마커** | 사건 인과 참여 금지 — 침투 0을 기계 강제 (F4) |

엣지는 필드로 인코딩: `supports`(증거→명제) · `refutes`(증거→거짓 진술) ·
`hides`(거짓 진술→명제) · `unlocks`(진술 결과→증거/플래그) · `proves`(지목 링크→명제).

## 2. 설계 원칙 — 이 사건이 아귀가 맞는 이유

- **범행의 반복 구조** — 14개월 전(넬)과 10월 9일(아이리스)은 같은 수법의 두 실행이다:
  유인/추적 → 탱크 → 연출. 반복이 루이즈의 패턴 증언("작년에도 그랬어요")과 L3(같은 사람,
  같은 자리)를 성립시킨다. 동기는 셀 FA1(접근 거부·민원 예고 — `motive` 태그, R2 결박).
- **조잡한 연출자** — 도일의 은폐는 매번 한 곳씩 어긋난다: 구두는 가지런한데 해치는 밖에서
  잠겼고(L1 자기모순), 심어 둔 열쇠엔 다이치의 필적 태그가 남았고(FB10, R3 결박), 일지는
  지우는 대신 써 넣어서 위조 자체가 서명이 됐다(FB11 → L2). **모든 지목 링크는 도일의 실수가
  아니라 도일의 "연출 습관"에서 나온다** — 증거가 우연히 남은 게 아니라 성격이 남긴 것.
- **물의 경로 = 사건의 경로** — 수압 하락(시점 특정) → 검은 물(발견) → 젖은 발자국(동선) →
  트랩 침전(오염 입증). 감각 레이어(E7)가 아니라 그래프의 골격이다.
- **결함 3건 수정 (MASTER-PLAN §3.3)** — ①프라이스 해고=14개월 전, 거주 4년 공존(FA9) ②도일
  동기=상습 접근·거부·민원 위협(FA1) ③roofkey=도일이 사후 침입 때 심음(FB10). 각각 factcheck
  R1·R2·R3가 회귀 감시한다.

## 3. 소각 경제의 데이터 기반 — 단일/이중 경로 배분 (E5가 소비)

| 증거 | 획득 경로 | 소각 시 연쇄 |
|---|---|---|
| `pressure-log` | **단일** — 다이치 S3 (TRUTH/DOUBT) | 잃으면 루이즈 S2 반박 불가 → `two-voices` 소실 → `footprints` 소실 → 루이즈 S4 반박 불가. **최대 연쇄** |
| `photos` | **단일** — 프라이스 S1 정답 | 잃으면 S3 자백 불가 → L3 불성립. "게임 최대의 손실" |
| `footprints` | **단일** — `two-voices` 플래그 출현 | 상류(pressure-log) 의존 |
| `water-log` | **이중** — 944호 수색 또는 S4 정답 | 심문을 망쳐도 수색으로 회수 가능 |
| 나머지 10종 | 수색/관찰 — 소각 무관 | 없음 |

설계 의도: 단일 경로 3종이 전부 **심문 성과**에 걸려 있어 "심문이 수사의 무게중심"이라는
코어(E1 U1)를 데이터가 강제한다. 최악 소각 시에도 L1은 성립(수색·관찰만으로) → 미제 엔딩
도달(F3 PASS). 배분 변경은 이 표가 아니라 case-graph.json을 고치고 factcheck로 재검증한다.

## 4. 소비 계약

- **script.js v2 이행(P0 티켓)**: `src/narrative/script.js`의 진술·증거 객체는 case-graph.json의
  id를 그대로 쓴다(`deitch.S2`, `register`). 원산지·반박 관계를 script.js에 중복 기재하지
  않는다 — 게임 코드는 대사·연출만, 관계는 그래프만.
- **2단 커널(E5 §4)**: 자유 서술 지목의 의미 매칭 대상이 FACT 노드의 `text`다. 명제가 문장
  단위로 존재하는 것이 스왑의 전제 조건 — 이 그래프가 곧 2단의 정답지다.
- **factcheck 실행 시점**: 내러티브 데이터 변경마다 (커밋 게이트, E9 §기계 게이트 총목록).

---

## [플레이어]

플레이어는 그래프를 보지 않는다. 그 효과를 겪는다 — 어느 방에서 무엇을 주워도, 언제 누구에게
무엇을 내밀어도 세계가 모순으로 무너지지 않는다. 민원 장부의 시각과 루이즈의 서명이 맞고,
발자국의 방향과 카펫의 얼룩이 맞고, 부검 사본의 한 줄이 14개월 전 그 밤과 맞는다. 옥상의
증거판에서 두 증거를 나란히 놓는 순간 링크가 철컥 맞물리는 감각 — "내가 알아냈다"는 확신은
이 아귀가 한 번도 어긋난 적 없기 때문에 성립한다. 그리고 괴담은 끝까지 괴담이다. 라디오가
뭐라고 속삭였든, 사건을 닫는 것은 언제나 물질이다.

## [구현]

- 진실원: `docs/design/case-graph.json` (스키마는 파일 `meta.schema` + 본 문서 §1).
- 검증: `node tools/factcheck.mjs` — S0(참조 무결성)·F1~F4·R1~R3. 전 항목 PASS가 커밋 조건.
- 필드 계약(요약): `obtain.via ∈ {search, observe, grant, spawn}` · `unlocks`의 outcome ∈
  {truth, doubt, lieCorrect, onLieWrong} · `burnable:true` = LIE+오답 시 진술 소각, 해당
  진술의 unlocks 영구 무효 · `reAct` = 재심문 가능 막.
- 규모 상한: FACT 노드 40개 이하 유지(현재 25) — 그래프 비대는 2단 커널 판정 배터리
  비용으로 직결된다. 추가는 "지목·반박에 참여하는가"를 통과해야 한다(F4가 고아를 차단).

## [판정]

- M6 개연성: factcheck F1~F4 + R1~R3 전건 PASS (기계). 현재 상태: **PASS** (R-A, 변이
  테스트 5종 검출 확인).
- M5 유니크니스(부분): §2·§3이 "구조 차별이 데이터 층에 실재"의 증거 좌표.

## [위임]

- 선행 의존: E1(코어 확정) · MASTER-PLAN §3.2 매트릭스(입력).
- 배타 소유 파일: `docs/design/E3-case-graph.md` · `docs/design/case-graph.json`.
- 수용 기준: factcheck 전건 PASS · 단일/이중 배분 표와 json 데이터 일치 · FACT ≤40.
- 권장 모델 클래스: 그래프 데이터 수정은 **단일 최강 모델**(연쇄 파급 판단 필요).
  script.js v2 기계 이행은 계약이 두껍고 factcheck가 수용 기준이므로 **외부 모델 가능**
  (E10 티켓 T-P0-03).
