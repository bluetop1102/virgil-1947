# 발사문 — 체험 재판정 (Fable 단독 세션 · 2차)

> 실행자: Fable 새 세션. **판정만 한다 — 코드 수정·발주·머지 금지.**
> 판정 대상은 **갱신된 배포본** `https://bluetop1102.github.io/virgil-1947/`
> (커밋 `6b7bec3`, 2026-08-09 배포. 실배포 검증 PASS — 부팅 성립·요청 실패 0·콘솔 0).
> 제출 **8/10**.

## 0. 이 세션이 왜 필요한가

네가 2026-08-08에 낸 1차 판정(`docs/reviews/fable-experience-2026-08-08.md`)이 게이트 1·2·3
**전부 불통과**였다. 그 판정을 받아 병렬 4세션이 지적 전건을 수정했고, 이제 **같은 눈으로
다시 걸어 무엇이 실제로 달라졌는지** 판정해야 한다.

**1차 판정문을 먼저 읽어라.** 네가 쓴 것이지만 이 세션은 그 기억이 없다. §0의 실측 5건,
§1의 G1~G10 "8+가 되려면" 조건, §2의 N1~N8, §3의 블라인드 판정이 이번 재판정의 대조군이다.

## 1. 먼저 읽는다

1. `docs/reviews/fable-experience-2026-08-08.md` — **1차 판정문(대조군)**.
2. `docs/RESUME.md` §3 — 기각된 가설 17건. 기전을 추정할 때 여기 적힌 것을 다시 세우지 마라.
3. `docs/AAA-RUBRIC.md` — 채점기. 판정은 이 항목으로만 한다.
4. `docs/HANDOFF.md` 하단 — 4세션 회수 기록과 **수용된 미달 항목**(아래 §3).

## 2. 무엇이 수정됐는가 (시간 낭비 방지용 선제공)

재발견에 시간을 쓰지 말라고 준다. **"고쳤다고 하니 통과"로 읽지 마라 — 검증하라는 목록이다.**

| 1차 지적 | 수정 내용 | 자가 실측 |
|---|---|---|
| §0-1 렌더 잠식 (제출 차단급) | HDR 하프플로트 오버플로 → `Inf*0=NaN` → 시간 이력(EMA) 영구 감염이 기전. ssr·bloom·taa·motionblur·volumetric·composite 6곳에 NaN/Inf 관문 | 재현 A(t=174)·B(t=119)·C(t=149)까지 노출 22.0 평탄·순흑 0.00%. 1차 때는 t=139에 상한 600 고정이었다 |
| §0-2 인트로 자막 4줄 | 자막 레이어를 베일 위로(z93) + **같은 프레임 clear 버그** + 타이핑 dur 하한 | canvas 잉크 픽셀 판정으로 4줄 전건 렌더 |
| §0-3 심문 이탈 누수 | 사거리 4.2m 이탈 시 세션 종료·선택지/지목 모드 해제. Escape 1차 동작을 제시 취소로 | 거리 9.22m에서 phase=idle, 이후 입력이 점수를 안 만든다. UI 상태기계 단언 신설(배터리 64→108) |
| §0-4 자막·선택지 겹침 | 선택지 쪽지를 bottom 6.5% → 13%로 세로 분리 | |
| §0-5 엘리베이터 무응답 | 거절 대사 한 줄 | |
| D3 타일링 | 벽지 얼룩 주기 파괴 · 대리석 8×8/결 확대 · 목재 도관 주파수 · 석고 균열 대비 | **미튼 얼룩 반복은 해소. 체커 타일·벽지 모티프 반복 인지는 여전히 가능** — 아래 §4 참조 |
| D4 플레이스홀더 | 라디오 전용 재질(놋쇠 다이얼·패브릭 그릴·바니시 목재) · 데스크 서랍 목재 패널 | |
| G8 화염 기둥 | 포스트 절제 + `wood.varnished.dark` clearcoatRoughness 0.15→0.30 | `lobby-wide` 펜던트 위 세로 띠 소멸. 남은 것은 헐레이션 글로우 |
| G1/G9·G10·G5·G6 | 샹들리에 광속 2500→2100·엘리베이터 820→520lm(주변 감쇠) · 크라운 몰딩 4단·웨인스코트·엘리베이터 아치·리드 기둥 · 바닥 젖음/마름 구획 · 소품 밀도 | |
| N2 텔 | 다이치 불안 텔을 읽히는 크기로 + 같은 신호의 2회차 변주 | |
| N3 로어 3종 | **배선은 있었는데 발화가 없었다** — 전 소스에 괴담 원문을 말하는 모듈이 0개였다. STORY §7 원문을 script.js LORE로 옮기고 자막으로 발화 | 완주 봇에서 `lore:heard` 3건 |
| N5 카메라 | 오답 뒤 반 발짝 진폭 확대 | |
| **인트로 나레이션**(사용자 체험 피드백) | 구판은 날짜·장소·수압·실종의 나열이라 "9층까지 물이 오르지 않는다"가 왜 중요한지 안 읽혔다. 화자를 1행에 명시하고 4행이 두 이상 신호를 하나의 의심으로 묶는 판으로 교체(E2 사양 동시 개정) | 완성행 렌더 3종 → 5종 |
| **자막 판독성**(사용자 체험 피드백) | 크기 상한 27px 해제(→36) · 글자 뒤 어둠 강화 · **타이핑이 글자마다 `show()`를 불러 페이드인이 매번 재시작되던 버그** 수정 | t=7 프레임 육안 선명 |

**기계 게이트 현황**: 심문 108/0 · burn 9/0 · unlocks 10/0 · factcheck 전 게이트 ·
`npm run shot` 판정 27/27·실패 0·**보류 6**(P2 미구현 레벨을 정직하게 분리) · 콘솔 0 ·
완주 봇 1막 PASS(`act:enter{act:2}` @1:04).

## 3. 수용된 미달 — 이미 알고 있으니 재발견에 시간 쓰지 마라

1. **노트 괴담 면 미구현** — E8이 선언한 면인데 `_list()`가 증거만 훑어 괴담이 노트에 안 남는다.
   직렬화가 `src/core` 잠김 영역이라 범위 밖으로 수용.
2. **E7 §3 RT60 사양 이탈 2건** — 로비 1.6 사양 대 실측 2.38 · 복도 0.9 대 1.50.
3. **`kit-mat.js` `WEAR_PATCH` 사문화** — `mesh(..., {wear})`의 러프니스 상승분이 전 프로젝트에서
   안 먹고 알베도만 산다. 한 줄이면 고쳐지나 28샷 전 마모 표면이 동시에 움직여 제출 이틀 전
   전역 회귀 위험이 커 등재만 하고 수용.
4. **천장 석고 균열 노이즈** — 대비를 절반으로 낮췄을 뿐 제거하지 못했다(기구 위치 제약).
5. **P2 레벨 4종 미구현** — 복도·942호·욕실·옥상. 걸을 수 있는 공간은 여전히 **로비 하나**다.

## 4. 이번 판정에서 반드시 답해야 할 것

1. **게이트 1·2·3 각각 통과/불통과.** 1차 대비 무엇이 올랐고 무엇이 그대로인지 항목별로.
   점수만 매기지 말고 **"8+가 되려면"을 이번에도 붙여라** — 남은 시간이 하루뿐이라
   그 조건이 곧 마지막 작업 지시가 된다.
2. **D3 실격이 해제됐는가.** 회수 세션의 육안 검수로는 단정하지 못했다 — 미튼 얼룩은 사라졌으나
   대리석 체커와 벽지 다마스크의 반복 인지가 여전히 가능해 보인다. **이 판정이 이번 세션의 핵심이다.**
3. **§0-1 잠식이 정말 사라졌는가.** 자가 실측은 재현 A·B·C 전부 음성이지만, 네 1차 발견은
   시점·동선 의존(t≈52~127)이었다. **다른 동선으로도 찔러봐라.**
4. **블라인드(게이트 3)**: 프레임을 고를 수 없는 조건에서 상용 게임으로 오인 가능한가.
   1차에서 즉답을 가른 것은 라디오·크래클 벽·광 기둥·미튼 얼룩이었다 — 그 넷이 사라진 지금은?
5. **제출 프레임 3종 추천.** 블라인드 비교에 낼 화면을 네가 골라라.

## 5. 판정 한계 (1차와 동일)

- **오디오는 헤드리스라 청감 불가**다. N7은 판정 보류하고, `docs/submission/audio-listen-check.md`
  (5항목 체크리스트)를 사람이 실행할 목록으로 인용만 해라.
- 포인터락이 헤드리스에서 거부되면 `player.yawT/pitchT` 직접 조정으로 대체한다(등가 입력).
  이동·상호작용·선택은 실제 키 입력으로 해라.
- 2막·3막 공간은 P2 범위라 미판정. N2 인물별 텔 4종도 1막 다이치 단독으로는 표본이 부족하다.

## 6. 금지

- **코드 수정·커밋·발주 금지.** 판정문만 쓴다.
- 기계 게이트 재실행 금지 — 실측은 위에 있다. 볼 것은 **그 게이트가 못 보는 축**이다.
  1차에서 네가 찾은 5건이 전부 전 게이트 통과 상태로 배포본에 살아 있었다.
- 실제 사건 피해자 재현 금지. 인물·사건은 전부 허구다.

## 7. 산출

`docs/reviews/fable-experience-2026-08-09.md`. 프레임 증거는 `docs/reviews/shots-fable-2/`.
1차 판정문과 **같은 표 구조**로 써라 — 대조가 가능해야 한다.
마지막에 사용자에게 **제출 가능 여부**를 한 문단으로 보고한다. 남은 하루에 무엇을 더 고칠지가
아니라 **지금 내는 것이 성립하는지**가 이번 판정의 실질이다.
