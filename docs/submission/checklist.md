# 제출 전 체크리스트 — 최종 관문

> 마감 **2026-08-10** · 리허설 **2026-08-08**. 마감 후 변경 불가이므로, 이 목록은
> "확인했다"가 아니라 **명령을 돌려 출력을 본 것**만 체크한다. 눈으로 훑고 체크하지 않는다.

## A. 기계 게이트 (명령 그대로 실행 · 전건 통과)

```bash
node tools/manifest-check.mjs          # 위임 매니페스트 정합 — 발견 0
node tools/factcheck.mjs               # 사실 그래프 F1~F4·R1~R3 — 전건 PASS
node tools/lint-contract.mjs           # 계약 린트 5종 — 위반 0                (T-P0-01 이후)
node tools/playthrough.mjs --fast      # 완주 봇 — 도달성·이벤트 성립          (T-P1-06 이후)
npm run build:pages                    # 상대 경로 빌드 — 에러 0
node tools/serve-check.mjs                              # 루트 배포 PASS
node tools/serve-check.mjs --prefix /virgil-1947         # 서브패스 배포 PASS
node tools/serve-check.mjs --url https://bluetop1102.github.io/virgil-1947/   # 실배포 PASS
```

- [x] 배포 3종 통과 (2026-08-06 실측: 루트·서브패스·실배포 전부 부팅 성립·요청 실패 0·콘솔 0).
- [ ] 위 8개 전건 통과. `npm run build`(base=`/`)가 아니라 **`build:pages`** 로 빌드했는가
      — 기본 빌드는 서브패스에서 404 5건으로 깨진다.

## B. 심사자 재현 — 시크릿 창 완주

브라우저 **시크릿 창**에서 배포 링크만으로 한다. 캐시·확장·로그인 상태가 결과를 가리지 않게.

- [ ] 링크 클릭 → 설치·계정·API 키 요구 0 으로 실행된다.
- [ ] 개발자 도구 콘솔에 **에러·경고 0**. (요건이 아니라 우리 실격 기준이다)
- [ ] 로딩 → 허구 고지문 → 타이틀 → 첫 입력 → 게임 진입이 끊기지 않는다.
- [ ] 저사양 리허설: 크로미움 개발자 도구 **CPU 4× 스로틀링 + `?q=low`** 로 완주 가능한가.
      (심사자 노트북이 M2가 아니다. 이 항목이 실질 탈락 방지선이다)
      *(2026-08-06 현재 **`low` 프리셋이 없다** — `?q=low` 는 경고 없이 `high` 로 폴백하고,
      `medium` 도 볼류메트릭·GTAO 를 끄지 않는다. `docs/HANDOFF.md` 등재분. 이 항목은
      프리셋 신설 전에는 통과 자체가 불가능하다.)*
- [ ] 다른 기기 1대에서 같은 링크로 재현. 내 캐시가 아닌지 확인.

## C. 콘텐츠 — 실격 사유 차단

- [ ] **배포본에 실존 업체·인물명 표출 0**. 확인 명령:
      `npm run build:pages && grep -roE '(CECIL|Cecil|세실)' dist/ | grep -v rapier`
      → 0건. `<title>` 과 부트 화면 대문짝을 눈으로도 확인한다.
      *(2026-08-06 현재 **부분 해소** — `index.html` 의 `<title>`·부트 대문짝은 고쳤다.
      번들에 남은 3건은 인게임 문자열이라 **플레이 중에 보인다**: 수사노트 표지 "세실 호텔",
      미제 엔딩 캡션·자막 "세실은 계속 영업했다". `src/**` 소유자 몫 — `docs/HANDOFF.md` 등재분.)*
- [ ] 시작 화면에 허구 고지문이 표시된다.
- [ ] 외부 에셋 0 — `git ls-files | grep -icE '\.(png|jpg|hdr|mp3|wav|glb|woff2?|ttf)$'` → 0.
- [ ] `docs/credits.md` 의 의존성 표가 `npm ls --omit=dev --depth=0` 실제 출력과 일치한다.

## D. 저장소

- [x] 전 이력 시크릿 스캔 통과 — 고유 텍스트 blob 748개/31.8MB, 검출 0 · 위험 경로 0.
- [x] **이력 재작성 하지 않았다** (squash·filter-repo 없음). `gh-pages` 만 산출물 전용 강제 갱신.
- [x] 구간 분할 push 완료 — 1구간(첫 커밋 1.78GB) 101초, 2구간(나머지) 즉시.
- [x] public 저장소 `bluetop1102/virgil-1947` — 심사계정 초대 불요.
- [x] GitHub Pages 가동(`gh-pages` 브랜치, 경로 `/`). 실제 공개 URL 검증 PASS.
- [ ] 마감 직전 **시크릿 창에서 사람이 직접** 한 번 더 연다 (자동 검증과 별개).
- [x] `scratchpad/`·`shots/`·`dist/` 추적되지 않는다(`.gitignore`).

## E. 제출물 5종 — 누락 하나가 심사 제외

- [ ] 1 · 플레이 가능한 빌드 + 소스 (GitHub 링크)
- [ ] 2 · 플레이 동영상 30~60초 (YouTube 링크) — **실플레이 화면. AI 조작·합성 없음**
- [ ] 3 · 게임 소개·설명 문서 PDF — 제목·한줄소개·게임 방법(목표·조작·종료조건)·실행 방법·링크
- [ ] 4 · AI 활용 기술 문서 PDF — 구조 설명·**주요 프롬프트·지시사항**·외부 에셋/오픈소스 출처
- [ ] 5 · 팀원 롤 기술서 PDF — **2인 이상만**. 1인 참가면 해당 없음
- [ ] PDF 3종의 링크가 전부 살아 있고, 클릭해서 열리는지 직접 눌러봤다.

## F. 링크 유지

- [ ] 배포 URL · 저장소 URL · 영상 URL 3개를 한 곳에 적어 두었다.
- [ ] **심사 종료까지 셋 다 유지**한다 — 저장소 비공개 전환, Pages 비활성화, 영상 삭제 금지.
- [ ] 마감 이후에는 어떤 것도 바꾸지 않는다.

---

## 리허설(8/8)에 반드시 포함할 것

B의 저사양 스로틀링 완주와 D의 구간 분할 push 는 **처음 해보면 반드시 시간이 걸린다.**
마감 당일이 아니라 리허설에서 한 번 통과시켜 둔다. 특히 2.0GB 저장소의 첫 push 는
네트워크와 GitHub 한도에 걸릴 수 있는 유일한 비가역 병목이다.
