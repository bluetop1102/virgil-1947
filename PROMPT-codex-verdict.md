# 《VIRGIL》 독립 판정 발주 — codex 전용 (별도 세션에서 실행)

*이 발주는 리서치 산출(`docs/research/experience-components.md`)이 존재한 뒤에만 유효하다.
**리서치를 수행한 세션과 반드시 분리해서 실행한다** — 자기 산출을 자기가 검증하면 판정이
아니다. 산출물은 `docs/research/codex-verdict.md` 하나이고, 디렉터가 여기에 채택·기각을
표기한 뒤 fable 발사문(`PROMPT-plan-v2.md`)의 입력으로 들어간다.*

프로젝트: /Users/kang-yunbyeong/Documents/WORK/Projects/cecil-hotel-noir

## 너의 역할

독립 판정자다. **문서를 고치지 마라. 코드를 만지지 마라. 축을 새로 지어내지 마라.**
너의 산출은 판정문 하나다. 이 프로젝트의 기획 세트는 이미 자체 루브릭(M1~M7)에서 7항목 중
5개가 만점인데 주인공이 없다 — 즉 **채점기가 못 보는 것을 보는 것**이 네가 불려 온 이유다.
관대하게 읽지 마라. 의심스러우면 결함으로 적고 좌표를 붙여라.

## 입력 (이 순서로)

1. `docs/research/experience-components.md` — 회자군/실패 대조군 대비로 뽑은 컴포넌트 목록.
2. `docs/design/E0-index.md` ~ `E10-production.md` — 기획 확정본(게이트 2 통과, 2026-08-06).
3. `docs/STORY.md` · `docs/design/case-graph.json` — 사건·대사·사실 그래프의 진실원.
4. `docs/AAA-RUBRIC.md` · `docs/design/graders/grader-M.md` — 현행 채점기.
5. 아래 [디렉터 진단 5건].

## 디렉터 진단 5건 (2026-08-06 실측 — 네가 반박할 대상이다)

1. 플레이어 캐릭터가 정의돼 있지 않다 — "형사"라는 직함뿐이고 E0 진실원 지도에 항목 자체가
   없다. 무성 주인공도 아니다(STORY §5에 "형사:" 대사 실재).
2. 호출 계기가 두 갈래이고 1막 시각과 어긋난다 — case-graph `FC1`·STORY §1은 "10월 11일
   아침 검은 물 → 호출"인데 1막은 같은 날 새벽 2시(E2·STORY §5.1). 실종 신고 주체도 없다.
3. 심문에 변주가 없다 — E5 판정표가 4인 전원 동일, 25분짜리 2막에서 규칙이 한 번도 안 바뀐다.
4. 감정 곡선을 소유한 문서가 없다 — E2는 밀도(무사건 3분 초과 0)만 관리한다.
5. 원인은 채점기다 — AAA-RUBRIC 서문 "판정 불가 항목은 삭제한다" + grader-M M3 규칙 3
   "코어 무기여는 존재 자체가 감점"이 체험 요소를 구조적으로 밀어냈다.

## 산출할 것 — `docs/research/codex-verdict.md`

### 1. 컴포넌트 × 현 세트 대조표

리서치 컴포넌트 **전건**에 대해 한 행씩. 열: `컴포넌트 | 판정 | 좌표 | 근거`.
판정은 넷 중 하나 — **충족**(우리 세트에 이미 있다, 파일·절 좌표 필수) / **미충족**(없다) /
**부분**(있으나 특정 조건에서만) / **무관**(우리 장르·규모에 적용되지 않는다, 이유 필수).
좌표 없는 "충족" 판정은 무효다. 문서에서 그 문장을 찾아 인용하라.

### 2. 코어 충돌 목록 — **이 절이 이 발주의 핵심이다**

리서치가 "있으면 죽고 없으면 사는 필수"로 분류한 컴포넌트 중, **채택하면 이 작품의 확정된
코어가 깨지는 것**을 골라내라. 이 프로젝트의 코어는 되돌릴 수 없음이고, 그것을 지키려고
내린 결정들이 이미 확정돼 있다 — 수동 저장 배제(E8 §4) · 엔딩 = 회차 종결로 "이어서" 소멸
(E8 §4) · 무힌트 불변(E5 §1) · 오답 페널티 없는 지목판(E5 §5) · 미제도 정식 엔딩(E1 U2) ·
점프스케어 0·초자연 실재 0(E7).

예컨대 리서치가 "재플레이·다회차 유도가 회자를 만든다"고 결론 냈다면, 우리는 그 경로를
설계로 봉쇄했다 — 그 컴포넌트는 **채택 불가**로 판정하고, 대신 같은 효과(회자)를 단일 회차
안에서 얻는 대체 경로가 리서치 근거 안에 있는지 찾아라. 충돌마다 다음 셋 중 하나를 골라
근거와 함께 적어라: **①코어 우선 — 컴포넌트 기각** / **②대체 경로 존재 — 좌표 제시** /
**③코어 재론 필요 — 디렉터 판단 사항으로 상신**(③은 남발 금지. 확정 게이트를 두 번 통과한
결정이다).

### 3. 진단 5건 검증

각 건에 **성립 / 부분 성립 / 반증** 판정과 좌표. 반증이면 우리가 놓친 문서 위치를 대라.
특히 1번(주인공)은 "무명 1인칭이 이 장르에서 정당한 선택인가"를 리서치 근거로 판정하라 —
정당하다면 결함은 "주인공 부재"가 아니라 "무명이 의도라는 선언의 부재"로 재정의돼야 한다.

### 4. 리서치가 놓친 것

우리 작품 고유의 사정(1947 느와르 · 50분 단일 회차 · 4인 심문 · 절차 생성 그래픽 · 웹 배포)
때문에 일반 담론에 안 잡히는 컴포넌트가 있으면 적어라. 없으면 "없음"이라고 쓰고 끝내라 —
채울 것을 찾느라 지어내지 마라.

### 5. 축 후보 추천 (구속력 없음)

위 1~4를 근거로, 체험 축 루브릭에 들어갈 항목 4~6개를 추천하라. 각 항목에 **근거 좌표**와
**판정 수단 아이디어**를 붙여라. 판정 수단을 못 대는 항목은 추천하지 마라 — 판정 불가 항목이
루브릭에서 잘려 나간 것이 애초에 이 사태의 원인이다. 이건 추천이지 확정이 아니다.
확정은 fable 세션의 Phase A와 디렉터 게이트가 한다.

## 금지

- 문서·코드 수정. 산출은 `docs/research/codex-verdict.md` 하나뿐이다.
- 총평("전반적으로 탄탄하다"). 항목별 판정 + 좌표만.
- 좌표 없는 단정. 문서에서 인용하지 못하면 "확인 불가"로 적어라.
- 확정 결정의 임의 재설계 — 재론이 필요하면 §2의 ③으로 상신만 한다.
