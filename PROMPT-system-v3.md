# 발사문 — 위임 시스템 v3 (P0 발주 개시)

> 실행자: Opus 새 세션. 역할 = P0 실발주 + 창작형 캘리브레이션 + 위임 개시 판정 완결.
> 선행: v1(A·B·D 구축) → v2(계약 닫힘·재전사) → **이 문서**. v1 §1 소유 경계·§8 예산은 그대로 산다.
> 앞선 두 발사문을 다시 읽을 필요는 없다 — 필요한 것은 §0·§1 에 옮겨 놨다.

## 0. 지금 서 있는 자리 (전부 실측)

| 축 | 상태 | 근거 |
|---|---|---|
| 매니페스트·검증기 | **완료** | 티켓 16장 · `manifest-check` **8규칙 전건 PASS · 발견 0** |
| 패킷 생성기 | **완료** | 패킷 16장 · `packet-gen --audit` **잔여 0** |
| 캘리브레이션 ① 기계형 | **완료 — 2에이전트 통과** | Codex·Grok 구조·게이트·프로세스 3축 전건 |
| 캘리브레이션 ② 창작형 | **미착수** | T-P1-02 — P0 선행 필요 |
| 제출 인프라 | **완료** | public 배포 가동 · 실배포 검증 PASS |
| 계약 공백 | **전건 닫힘** — 1차 5건(`27010a1`) · 2차 4건(`66f7566`) | 매니페스트 재전사 완료 |

- 배포 https://bluetop1102.github.io/virgil-1947/ · 소스 https://github.com/bluetop1102/virgil-1947
- 검증: `node tools/serve-check.mjs --url https://bluetop1102.github.io/virgil-1947/`

**측정된 핵심 사실**: 패킷 1장으로 서로 다른 두 외부 에이전트가 질문 0·소유 밖 편집 0 으로
같은 구조를 만들고 게이트를 통과했다. 그리고 **계약에 판별 규칙이 들어가자 1회차에서
갈렸던 지점이 2회차에 수렴했다**(`tools/calibration/report.md` §1.3). 위임의 전제가 성립한다.

## 1. 필독 (이것만)

1. `AGENTS.md` — 안전 규칙·샷 하네스 규약·**병렬 커밋 규약**.
2. `tools/calibration/report.md` — §1 2에이전트 실측 · §3 결함 역반영 · §4 판정.
3. `tools/calibration/protocol.md` + `prompt-pilot.md` — 실행 규약과 투입 원문.
4. `docs/design/E9-gates.md` §2 판별 규칙 7줄 — T-P0-01 발주 전 반드시. 계약이
   여기까지 정해 놨다는 것을 알아야 패킷의 forbidden 을 이해할 수 있다.
5. `docs/submission/README.md` — 제출 현황.

## 2. 소유 경계 (승계 — 변경 없음)

- **편집 금지**: `docs/design/E*.md` · `STORY*` · `ROUNDS-PLAN.md` · `AAA-RUBRIC.md` ·
  `ARCHITECTURE.md` · `src/**` · 기존 `PROMPT-*.md`.
  → **예외**: 외부 에이전트가 만든 `src/**` 산출을 본체에 머지하는 것은 이 세션의 일이다(§4.3).
- **배타 소유**: `data/manifest/**` · `packets/**` · `tools/{manifest-check,packet-gen,serve-check}.mjs` ·
  `tools/calibration/**` · `docs/credits.md` · `docs/submission/**` · 배포 설정.
- **커밋 규약**: `git commit -a` · 무인자 `git add -A` **금지**. 자기 소유 경로를 명시해
  스테이지한다. 이 저장소는 상시 2세션 이상이 동시에 돈다(커밋 `379f7be` 충돌 사고).

## 3. 발주 순서 — 막힌 것 없음

계약 4건이 전부 닫혔다(`66f7566`). 훅 마커는 `# virgil-contract-lint managed` 고정
문자열로 확정됐고(변형 금지 — 두 에이전트가 다른 마커를 채택했던 지점), `--staged`
수집·허용 경로(`atmosphere.js` + `atmo/**`)·주석 취급도 실행 가능한 수준으로 정해졌다.
매니페스트 재전사 완료 — `manifest-check` **8규칙 PASS · 발견 0**.

| 순서 | 티켓 | 비고 |
|---|---|---|
| 1 | **T-P0-01** 계약 린트 | 다른 4장의 수용 기준이 이 도구를 부른다 — 먼저 |
| 1 | **T-P0-05** 텔 상관 · **T-P0-06** low 프리셋 | 린트 무관 — T-P0-01 과 **병렬 가능** |
| 2 | T-P0-04 → T-P0-02 | 위상 순서(분할 후 청소). T-P0-01 머지 후 |
| 2 | T-P0-03 | 표출명 3건 포함. T-P0-01 머지 후 |

T-P0-06 은 저사양 리허설(8/8)을 막고 있으니 1군에 반드시 넣는다.

## 4. 할 일

### 4.1 P0 1군 실발주 — T-P0-01 · 05 · 06 (지금. 승인 완료)

**파일럿과 다르다: 이건 실제 산출을 본체에 머지하는 발주다.** 조건은 같되 결과를 쓴다.

```bash
node tools/packet-gen.mjs --all && node tools/packet-gen.mjs --audit   # 잔여 0 확인 선행
W=/Users/kang-yunbyeong/Documents/WORK/Worktrees
for t in 01 05 06; do git clone --local . $W/p0-$t; done   # 워크트리 금지(protocol.md §2)

# 투입 원문: tools/calibration/prompt-pilot.md — <PACKET> 만 바꾼다.
# 실발주이므로 "추측하고 기록" 예외 문단은 **빼고**, 패킷 §10.1
# (결정 못 하면 CONTRACT_CHANGE_REQUEST 로 반환) 원칙을 그대로 살린다.
codex exec -C $W/p0-01 -s workspace-write --skip-git-repo-check -o out-01.md "<프롬프트>"
```

- 셋은 서로 독립이라 **동시에 돌려도 된다**. 단 세션 한도가 가까우면 직렬로(§7).
- T-P0-01 은 캘리브레이션에서 이미 2벌이 나와 있다 — **새로 발주하지 말고 §4.3 으로
  머지하는 것이 싸다.** 계약이 그 뒤 바뀌었으니(마커 규약 등) 머지 후 새 수용 기준으로
  재검증하고, 미달이면 그때 재발주한다.
- 회수: 패킷 §8 수용 기준을 **그 클론 안에서 직접 실행**해 출력을 본다.

### 4.2 ~~Fable 답신 후 재전사~~ — **완료**

계약 4건 반영(`66f7566`) → `T-P0-01.json` forbidden 을 판별 규칙 7줄 준수로 교체하고
훅 공존 수용 기준(남의 훅 exit 1 → 자기 훅 멱등 갱신 exit 0)을 추가했다.
부수: 검증기 R6 의 시스템 바이너리 목록에 `printf`·`chmod` 등 누락분을 보강했다
(새 수용 기준을 R6 가 잡아낸 것 — 검사기가 제 일을 했다).

검증: `manifest-check` 8규칙 PASS·발견 0 · 변이 12종 12/12 검출 유지 · `--audit` 잔여 0.

### 4.3 산출 머지 규약 (외부 산출을 본체로 들일 때)

캘리브레이션 산출물 2벌이 이미 있다 — 채택 기준은 **계약 준수 동률이면 단순한 쪽**:

| | Codex | Grok |
|---|---|---|
| `tools/lint-contract.mjs` | 475줄 / 함수 25 | **386줄 / 함수 16** |
| 클론 | `Worktrees/cal-codex-P0-01` | `Worktrees/cal-grok-P0-01` |

머지 절차: 해당 파일만 본체로 복사 → 본체에서 수용 기준 재실행(클론에서 통과한 것이
본체에서도 통과하는지) → 자기 소유 경로만 스테이지 → 커밋. **훅은 커밋하지 않는다**
(`.git/hooks` 는 추적 대상이 아니다).

### 4.4 창작형 파일럿 ② (P0 반영 후)

`T-P1-02` 다이치 리그. A2 가 `lint-contract.mjs` 를 부르고 A1 이 샷을 찍으므로
T-P0-01 머지가 선행이다. 이게 끝나야 판정 조건 2가 채워진다.

## 5. 판정 — "위임 개시 가능" (protocol.md §6)

| 조건 | 현재 |
|---|---|
| 1. 기계형에서 전 에이전트 3축 통과 | **충족** |
| 2. 창작형에서 구조·프로세스 통과 | 미실행 — §4.4 |
| 3. 결함 전건 역반영 | **충족** — 1차 5/5 · 2차 4/4 |

**기계형 위임은 열렸고 계약 공백은 전부 닫혔다.** 남은 조건은 창작형 검증 하나뿐이다.

## 6. 남은 제출 차단

| 차단 | 닫는 경로 |
|---|---|
| 인게임 표출명 3건 | **T-P0-03** (티켓화 완료 — 고아 항목 아님) |
| `low` 프리셋 부재 | **T-P0-06** (§4.1 로 지금 닫을 수 있다) |
| 게임 미성립 | P0 6장 → P1 10장 |

## 7. 예산·안전

- 토큰 상한: 캘리브레이션 잔여 + P0 발주 = 500만(v1 §8·E9 §3 P0 기준 승계).
- **한도 근처에서 팬아웃 금지, 직렬 전환.** v1 에서 서브에이전트가 세션 한도로 죽은 실사례.
- 외부 에이전트 실행은 실비 — 승인 범위: 파일럿 ①·② · P0 발주(사용자 승인 완료).
- 이력 재작성(squash·filter-repo) 금지 — 제출 요건 정면 위반. `gh-pages` 만 산출물 전용 강제 갱신.
- **grok 권한은 열려 있다** — `.claude/settings.json` 의 `Bash(grok:*)`. 막히면 그 파일부터 본다.
- 완료 보고에 증거 첨부: `manifest-check` 출력 · `--audit` 잔여 · 각 수용 기준 명령 출력.
