# 발사문 — 대본 개고 (Codex 실행용 · 사용자 지시 2026-08-10)

> 목적: 대사·나레이션을 **사람이 말하는 것처럼, 연역적으로**(형사의 독백은 관찰→추론→결론의
> 사슬로) 다듬는다. 구조·시스템은 건드리지 않는다 — **내용만.**

## 1. 수정 대상 (이것만)

- `src/narrative/script.js` — **데이터 전용 파일**(코드 없음). 대사·나레이션·로어 원문.
- 인트로 나레이션 4줄이 script.js 밖에 있으면(`cinematics.js` 상수) 그 문자열 상수만.

## 2. 불변 계약 — 어기면 기계 게이트가 잡거나, 더 나쁘면 조용히 깨진다

1. **id 불변** — 진술·대사 id 는 `data/case-graph.json` 과 1:1 결박. 키 추가·삭제·개명 금지.
2. **진위 불변** — 각 진술의 truth/거짓 구분, 반박 증거 매핑은 사건 구조다. 문장을 바꿔도
   "무엇이 거짓이고 어떤 증거로 반박되는가"는 그대로여야 한다.
3. **camera 필드 불변** — 진술별 push/pull/slide/low 지시는 연출 계약(S-B·S-I).
4. **사실 결박 유지** — factcheck(`node tools/factcheck.mjs`)가 검사하는 사실 관계
   (날짜·객실·수압·인물 동선)를 문장에서 빼먹지 마라. 특히 인트로 4줄은 "날짜·장소·실종·수압이
   하나의 의심으로 묶이는 판"이라는 설계 의도(E2)가 있다 — 연역적 개고와 방향이 같다.
5. **로어(괴담) 원문** — STORY §7 결박. 톤은 시대 구어·간접 공포 유지.
6. **톤 계약** — 다이치: 단문·존대·회피("제 기억으론"). 형사 독백: 하드보일드 1인칭.
   이모지·현대 구어·영어 낱말 금지(1947).
7. **길이** — 자막 조판은 어절 개행이 있으나 한 진술이 지금의 1.5배를 넘으면 낭독 타이밍이
   밀린다. 문장은 다듬되 분량은 ±30% 안.

## 3. 검증 — 수정 후 전건 통과해야 커밋

```bash
node tools/factcheck.mjs               # 사실 그래프 전 게이트
node tools/test-interrogation.mjs      # 108/0
node tools/playthrough.mjs --fast --act 1   # 완주 + 괴담 3건 + 콘솔 0
```
+ 인트로·심문 자막 실렌더 확인(대본 바뀐 프레임 재촬영 — `tools/judge-probes/probe-tell.mjs`
재사용 가능, 어절 개행 정상 확인).

## 4. 커밋

pathspec 커밋만: `git commit -m "feat: 대본 개고 — 구어·연역 서술" -- src/narrative/script.js`
(cinematics 상수를 만졌으면 그 파일 추가). 이력 재작성 금지.
