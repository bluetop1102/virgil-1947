# 발사문 S-H — 조명·노출·물성 마무리 (J1②·J2·J3② 지원)

> 실행자: Claude 새 세션. 회수 **8/10 14:00** — 이후 게이트가 통합·재판정·최종 배포한다.
> 근거: 3차 재판정 `docs/reviews/fable-experience-2026-08-09-r3.md` §1 J1·J2 "9점이 되려면" +
> §2 "세 축의 마지막 한 걸음은 새 기능이 아니라 기존 자산의 조명·구도 재조정" + HANDOFF 미처리분.
> 3차가 "오전 회귀 위험" 때문에 보류를 권했던 축이다 — **사용자가 2026-08-10 완결을 지시해
> 해제됐다.** 그래서 이 발주의 제1 규율은 **항목 단위 pix diff 게이트**다. 회귀를 만들면
> 안 하느니만 못하다.

## 0. 먼저 읽는다

1. 3차 재판정 §1 J1·J2·J3, §0-2(램프 시선 경쟁 실측 — 휘도 1.56배·평탄면 18.25%).
2. **`docs/RESUME.md` §3 — 기각 가설 17건·A/B 오염 3종.** 그리고 `tools/judge-probes/common.mjs`
   헤더의 하네스 함정 3건.
3. `docs/HANDOFF.md` — 램프 역광(위치 이동 금지 제약)·엣지 스펙큘러 AA 처방·wood.varnished 반환.
4. `AGENTS.md` — 커밋 규약.

## 1. 소유 파일 (배타적 — S-E가 src/ui 2파일·frames·배포, S-I가 narrative·chars, S-J가 gameplay·hud·audio 를 동시에 만진다)

```
src/render/**  src/world/**  src/materials/**
shots/_baseline  shots/base-r3 (재동결은 게이트와 협의)
```

## 2. 작업 (항목마다: 수정 → 재촬영 → pix diff → 커밋. 실패 시 되돌리고 다음)

### P0-1. 와이드 오토노출 앵커 하향 [2h] — J2 최대 지렛대
3차 실측: 와이드 3구도 노출이 들려 천장이 화면 최밝면이 되고 키/필/림 분리가 사라졌다
(mean 91~95). **대조군 `docs/reviews/shots-judge/j2-lobby-wide.jpg` 의 광량이 정답에 가깝다** —
천장은 어둠에 남기고 샹들리에·창 사선만 살려라. 검증: `frame-05`·`fr-70` 구도 재촬영 mean 이
60~75 대로 내려오고, 블라인드("이 스크린샷은 상용 게임인가")에서 "데모" 즉답이 사라지는가.
주의: 전역 앵커를 건드리면 심문·근접 프레임이 같이 어두워진다 — 대조군은 그 값으로도 심문이
성립했으니 pix diff 로 심문·데스크 샷 회귀를 반드시 본다.

### P0-2. 엣지 스펙큘러 AA — 이방성 소거 파급 회복 [2.5h]
3차 §1 J2: 소거의 대가가 코퍼 보를 넘어 데스크 서랍(회색 스펙클 판)·카운터 상판(직조 섬유)·
원거리 기둥(광택 상실)까지 번져 있다. HANDOFF 등재 처방(레시피 `toks` 항) 그대로 —
이방성을 살리되 흰 필라멘트 앨리어싱만 죽인다. 검증: `fr-41` 근접 재촬영에서 니스 목재 판독 +
와이드에서 필라멘트 0(블라인드 대조 — 3차 채점 원문과 같은 질문).

### P0-3. 램프 개구 회전 [1.5h] — J3 첫째 조건
HANDOFF 처방: 개구를 데스크 안쪽으로 돌려 손 높이 대비 회복. **램프 위치 이동은 금지**
(S-B 회피 계산 전제). 회전 후 심문 컷에서 ①손 실루엣 해소 ②램프 볼 휘도/얼굴 휘도 비가
1.56 → 1.1 이하 ③S-B 카메라가 여전히 얼굴을 잡는가(`PROBE_OUT=shots/judge-sh
node tools/judge-probes/probe-tell.mjs` 재실행 — 3항목 PASS 유지 필수).

### P1-4. 이양 직후 명암 연결 [1.5h] — J1 둘째 조건
이양 순간 화면 7할이 밝은 벽지(mean 93.8)라 타이틀의 명암이 끊긴다. 처방 후보(택1):
`cinematic:end` 수신 시 노출을 낮게 잡고 2~3초에 걸쳐 적응 복귀(render 소유로 가능) —
어둠에서 눈이 트이는 연출. 검증: 이양 +0.5s 프레임 mean 이 60 이하에서 시작해 3초 내 정상 복귀.

### P2-5. 앙각 석고 균열 [여력 시만] — 3차 "미해소" 항목. 1시간 박스, 넘기면 수용 유지.

## 3. 검증 요약 (완료 조건)
항목별 pix diff 회귀 0(의도 하향 판독 3조건 문서화) · probe-tell 3항목 PASS 유지 ·
probe-frames 기계 2항목 PASS · `npm run shot` 전 샷 성공 · 완주 봇 PASS · 콘솔 0 ·
블라인드 채점(와이드 즉답·근접 재질·손 실루엣) 원문 인용.

## 4. 커밋·보고
pathspec 커밋(항목 단위). HANDOFF append만. 완료 보고: 항목별 수치(앵커 값·회전 각·toks 값)와
전/후 프레임 경로, 포기 항목. **배포는 하지 마라 — 게이트가 통합 후 한다.**
