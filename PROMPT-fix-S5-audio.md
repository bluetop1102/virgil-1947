# 발사문 S5 — 오디오 (AUDIO)

> 실행자: Claude 새 세션. 제출 **8/10**.
> 발주 근거: 사용자 실플레이 체험 피드백 — **"효과음이랑 BGM이 적당하지 않은 것 같다"**.
> 이 축은 fable 판정 2회 모두 **헤드리스라 청감 불가로 보류**됐다. 기계가 못 본 유일한 축이다.

## 0. 먼저 읽는다

1. `docs/design/E7-presentation.md` §3 — 오디오 사양(공간별 RT60·룸톤·침묵 연출).
2. `docs/submission/audio-listen-check.md` — S3가 만든 5항목 청감 체크리스트. **네 산출물이 이걸 갱신한다.**
3. `docs/reviews/fable-experience-2026-08-08.md` §2 N7 — 판정 보류 사유와 기계 게이트 통과 기록.
4. `AGENTS.md` — **외부 에셋 다운로드 금지. 모든 오디오는 절차 생성이다.** 이 원칙은 이 프로젝트의
   기술 정체성이자 제출물 4번(AI 활용 기술 문서)의 재료다. 샘플 파일을 받아오지 마라.

## 1. 소유 파일 (배타적)

```
src/audio/engine.js  dsp.js  graph.js  ir.js
tools/test-audio.mjs
docs/submission/audio-listen-check.md
```

**남의 파일** — 고쳐야 하면 `docs/HANDOFF.md`에 등재하고 자기 소유분만 진행한다.
같은 워킹트리에서 다른 세션이 동시에 돌 수 있다(S6 타이틀 = `src/ui/title.js`).

## 2. 현황 — 실측

- **1막에 BGM이 없다.** `engine.js:352 _music(kind)`가 `act === 3`과 엔딩 시네마틱에서만 켜진다
  (`engine.js:300,311`). 즉 **플레이 첫 35분이 앰비언스만으로 간다.**
- **sfx가 5종이다** — `ui.tick` · `paper.pickup` · `water.drip` · `radiator.tick` · `pipe.knock`.
  50분 게임의 상호작용 밀도에 비해 얇다.
- **RT60이 E7 §3 사양에서 벗어나 있다**(S3 반환분): 로비 사양 1.6 대 실측 2.38 ·
  복도 사양 0.9 대 실측 1.50. S3는 "들어보지 않고 잔향을 못 바꾼다"며 청감으로 넘겼다.
- 기계 게이트 `tools/test-audio.mjs --roomtone`은 통과 중이다 — **통과가 좋은 소리를 뜻하지 않는다.**

## 3. 작업

### P0. 1막 BGM

50분 중 35분이 무음악이다. 다만 이 게임은 **조용한 공포**가 원칙이라(점프스케어 금지·초자연 실재
금지) 음악을 깔면 톤이 깨질 위험이 있다. **"음악을 넣는다"가 아니라 "언제 들어오고 언제 빠지는가"를
설계하라.** 최소한 다음 세 자리는 판단이 필요하다.

- 타이틀 — 지금은 벨 소리 전까지 무음이다. 첫인상을 만드는 자리다.
- 인트로 30초 — 물소리·타자기 타건음이 이미 있다. 저음 드론이 더해질 자리인지, 이미 충분한지.
- 심문 — 침묵 연출이 이미 설계돼 있다(오답 룸톤 −6dB). 음악이 들어오면 그 설계를 해친다.

### P0. sfx 확충

발소리(바닥 재질별 — 대리석/카펫/타일 구획이 S1 작업으로 실제로 나뉘어 있다) · 문·서랍 ·
종이 넘김 · 엘리베이터 격자문 · 라디오 다이얼 · 시계추. **상호작용이 있는데 소리가 없는 지점을
먼저 훑어라** — `qaTarget`이 걸린 오브젝트가 목록이다.

### P1. RT60 사양 정합

로비 2.38 → 1.6 · 복도 1.50 → 0.9. `ir.js`가 임펄스 응답을 절차 생성한다.
**사양이 옳은지도 함께 판단하라** — 1947년 대리석 로비의 실제 잔향이 1.6초보다 길 수 있다.
사양이 틀렸다고 판단하면 E7 개정을 `docs/HANDOFF.md`에 제안해라(직접 고치지 말 것).

## 4. 검증 — 이 세션의 특수 조건

**헤드리스로는 소리를 판정할 수 없다.** 기계 게이트는 "발화됐는가"만 본다.

```bash
node tools/test-audio.mjs            # 기존 배터리 — 깨뜨리지 마라
node tools/test-audio.mjs --roomtone
npm run dev                          # 사람이 듣는다
```

그래서 **네 산출물의 절반은 `docs/submission/audio-listen-check.md` 갱신이다.**
사용자가 헤드폰으로 5~10분 안에 확인할 수 있는 체크리스트여야 한다 — 항목마다
**어디서 · 무엇을 들어야 하고 · 무엇이 잘못된 소리인지**를 적어라. "잔향이 자연스러운가" 같은
문항은 쓸모없다. "로비 중앙에서 박수 한 번 — 꼬리가 1.5초 안에 끊기는가"처럼 판정 가능해야 한다.

파형·스펙트럼을 파일로 떠서 눈으로 보는 것도 유효한 보조 수단이다(`OfflineAudioContext` 렌더 →
PNG). 소리를 못 듣는 대신 **볼 수 있는 증거**를 만들어라.

## 5. 커밋

**같은 워킹트리에서 다른 세션이 동시에 돌 수 있다. git 인덱스는 공유된다** —
`git add` 를 쓰지 말고 pathspec 커밋을 써라:

```bash
git commit -m "feat: 1막 룸톤·모티프" -- src/audio/ docs/submission/audio-listen-check.md
```

- `index.lock` 충돌은 정상이다 — 2~3초 뒤 재시도.
- `git status` 에 남의 수정이 보여도 **`git stash`·`git checkout .`·`git reset --hard` 금지.**
- 커밋 후 `git show --stat HEAD` 로 자기 파일만 들어갔는지 확인.
- **이력 재작성 금지**(제출 요건). `Math.random`/`Date.now` 직호출 금지 — `core/util.js`의 `rng`.
- 파일당 500줄 상한이 계약 린트로 강제된다. `engine.js`는 402줄이라 여유가 적다.

## 6. 완료 보고

- **무엇을 어떻게 만들었는지**(절차 생성 방식 — 어떤 신스·필터·IR 구성인지). 제출물 4번의 재료다.
- 사람이 들어야 할 항목과 그 판정 기준.
- 기계로 증명한 것(배터리·파형)과 사람 귀에 남긴 것을 **명확히 갈라서** 보고해라.
