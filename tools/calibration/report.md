# 교차 에이전트 캘리브레이션 — 리포트

> 실행 규약: [protocol.md](protocol.md). 이 리포트가 **"위임 개시 가능" 판정의 근거**다.
> 채워지지 않은 표는 판정 근거가 아니다 — 미실행은 미실행으로 남긴다.

**상태: 파일럿 ① 2에이전트 대조 완료 (Codex · Grok, 동일 패킷).** 파일럿 ② 미착수.

---

## 0. 선행 시도 기록

### 0.1 무질문 게이트 (내부 서브에이전트) — 미완

캘리브레이션과 같은 측정 장치를 내부 서브에이전트로 먼저 돌렸다. **세션 토큰 한도로 조기
종료**돼 측정값이 없다(패킷 완독·착수까지는 질문 0). 통과로 치지 않는다. §1 이 대체한다.

### 0.2 Codex 1회차 (구 패킷) — 대조군으로 보존

결함 5건을 고치기 **전** 패킷으로 돈 회차다. A1~A3 통과·질문 0·소유 밖 편집 0 이었고,
여기서 나온 지적 6건이 §3 의 수정 원료가 됐다. **현재 대조에는 쓰지 않는다** — 다른
지시서를 받은 회차라 비교 대상이 아니다. 기록은 결함 수정이 무엇을 바꿨는지의 증거로 남긴다.

주요 산출: 추측 4건(검사 범위 · 표출 판별법 · 500줄 줄번호 · 훅 공존 정책) → 그중 3건이
**계약 공백**으로 판정돼 E9 §2 에 판별 규칙 3줄이 신설됐다(Fable `27010a1`).

### 0.3 기계적 자기완결 감사

```
node tools/packet-gen.mjs --audit
```

| 시점 | 잔여 § 좌표 |
|---|---|
| 폐포 도입 전 | 202건 (15장 합계) |
| 폐포 도입 후 | **0건 (16장 전부)** |

---

## 1. 파일럿 ① 기계형 — `T-P0-01` 계약 린트 커밋 훅

**투입 조건 (양쪽 동일)**: 독립 로컬 클론(HEAD `6776218`) · 소지품 `packets/PACKET-T-P0-01.md`
1장 · 문서 열람 금지 · 질문 대신 추측 기록. 패킷은 계약 반영 후 재생성분(1,307줄).

```bash
codex exec -C $W/cal-codex-P0-01 -s workspace-write --skip-git-repo-check "<프롬프트>"
grok --cwd $W/cal-grok-P0-01 --permission-mode acceptEdits --no-subagents --output-format plain --prompt-file <파일>
```

### 1.1 통과표 (명령 실측)

| 축 | 기준 | Codex | Grok build |
|---|---|---|---|
| 구조 | 소유 파일 2개 정확히 일치 (`tools/lint-contract.mjs`·`tools/install-hooks.mjs`) | **통과** | **통과** |
| 구조 | 규칙 5종 전부 구현 | **통과** | **통과** |
| 구조 | 검사 범위가 계약(E9 §2)과 일치 — `src/**`·`tools/**`·`index.html` 의 `.js`/`.mjs`/`.html`, 500줄은 `.js`/`.mjs` 한정 | **통과** | **통과** |
| 구조 | `lint-allow: display-name` 화이트리스트 구현 | **통과** (2건) | **통과** (5건) |
| 게이트 | A1 `--self-test` — 위반 5종 각 exit 1, 청정 exit 0 | **통과** exit 0 | **통과** exit 0 |
| 게이트 | A2 훅 설치 + 실행 권한 | **통과** exit 0 | **통과** exit 0 (멱등 확인 출력) |
| 게이트 | **A3 차단 경로 왕복** — 위반 스테이지 → exit 1 → reset → exit 0 | **통과** 1→0 | **통과** 1→0 |
| 프로세스 | 질문 0 (되묻기 없음) | **통과** | **통과** |
| 프로세스 | 소유 밖 파일 편집 0 | **통과** | **통과** |
| 프로세스 | 추측 기록 제출 | **통과** 4건 | **통과** 7건 (재요청 후 — §1.4) |
| 품질 | 구현 규모 | 475줄 / 함수 25 | 386줄 / 함수 16 |
| 품질 | 출력 포맷 | `[rule] file:line detail` | `rule: file:line: detail` |

**앞의 셋(구조·게이트·프로세스)이 전건 통과다.** 품질 축의 차이(줄수·포맷·화이트리스트 개수)는
규약 §3 대로 기록만 하고 실격으로 치지 않는다.

### 1.2 A3 왕복 실측 (새 수용 기준의 핵심)

```
codex: 위반 스테이지 exit=1 · reset 후 exit=0  → 통과
grok : 위반 스테이지 exit=1 · reset 후 exit=0  → 통과
```

구 A3(깨끗한 인덱스 exit 0 확인)로는 훅이 실제로 막는지 알 수 없었다. 강화된 기준에서
양쪽 다 차단 경로가 재현된다.

### 1.3 계약 수정의 효과 — 측정된 수렴

Codex 1회차의 추측 3건(검사 범위 · 표출 판별법 · 훅 공존)이 계약 공백이었고, E9 §2 에
판별 규칙이 들어간 뒤 **양쪽이 같은 범위를 구현했다** — 계약이 실제로 수렴을 만든 것이
이번 회차의 가장 큰 소득이다.

| 항목 | 계약 명시 후 | 결과 |
|---|---|---|
| 검사 범위 | `src/**`·`tools/**`·`index.html`, 500줄은 `.js`/`.mjs` | 양쪽 일치 |
| 표출 판별 | 리터럴·HTML 텍스트 전수 + `lint-allow` 화이트리스트 | 양쪽 구현 |
| 훅 공존 | 자기 산출이면 멱등 갱신, 아니면 exit 1 | 양쪽 구현 |

### 1.4 Grok 보고 형식 미준수 (프로세스 관찰)

Grok 은 1차 실행에서 작업은 완수했으나 **최종 보고를 지정 형식으로 내지 않았다** —
진행 서술 1줄만 반환했다. 같은 클론에서 보고만 재요청해 §2~§4 를 회수했다(작업 재실행 없음).

이건 패킷 결함이 아니라 **투입 프롬프트/CLI 출력 계약의 문제**다. Codex 는 `-o <파일>` 로
최종 메시지를 따로 받는 경로가 있고, Grok 은 `--output-format plain` 이 진행 로그를 덮어썼다.
protocol.md §4 에 회수 방법을 보강했다.

---

## 2. 파일럿 ② 창작형 — `T-P1-02` 다이치 리그

**완료 (2026-08-06, P0 4.5장 머지 후 실행).** 투입: 독립 클론(HEAD `b9c66c3`) · 패킷 1장 ·
파일럿 프롬프트(추측 예외 포함). Grok 은 하네스 제약으로 5회차에 완주(§5.2 #31).

| 축 | 기준 | Codex | Grok build |
|---|---|---|---|
| 구조 | `src/chars/rig.js` 단일 소유 준수 | **통과** (단일 커밋 `a6db67d`) | **통과** (단일 파일 · 커밋 전 종료 — #31) |
| 구조 | 모듈 계약(default export·order 30) | **통과** | **통과** (ORDER=30) |
| 구조 | `room:changed` 구독만 · 이벤트 발화 0 | **통과** (구독 1·발화 0) | **갈림** — 발화 0·이름 발명 0이나 기존 이벤트 3종 추가 구독(qa:state·interrogation:start·game:pause) → **계약 공백 #30** |
| 게이트 | A1 샷 성공 · 콘솔 0 · 실루엣 판독 | **통과** exit 0 · console [] · 원형 안경+단추 조끼 판독(육안) | **통과** gate true · console [] · 사각 안경+조끼 판독(육안) |
| 게이트 | A2 계약 린트 — rig.js 행 0 | **통과** 0행 | **통과** 0행 |
| 프로세스 | 질문 0 · 소유 밖 편집 0 | **통과** | **통과** |
| 프로세스 | 추측 기록 제출 | **통과** 6건 | **통과** 7건 (보고 재요청 후 — §1.4 절차 재사용) |
| 품질 (기록만) | 실루엣 인상·소품 해석 | 288줄 · 휴머노이드 실루엣·옷깃·단추 분리 | 396줄 · 박스 프리미티브 몸통 — 식별은 되나 인상 열세 |

**측정 결과**: 구조 3축 중 2축·게이트·프로세스 전건이 양쪽 수렴. 갈린 것은 이벤트 구독
폐집합 여부 하나 — 패킷 §6 은 "표에 없는 이름을 만들지 않는다"(어휘 통제)만 명시하고
**열거 외 기존 이벤트 구독의 허용 여부를 정하지 않았다**. 정확히 §1.3 패턴의 재현이다:
계약이 정한 축(소유·모듈 계약·발화 금지)은 수렴했고, 계약이 비운 축(구독 집합)은 갈렸다.
품질 발산(리그 해석)은 창작형에서 정상 — 구조 갈림만 결함으로 잡는 측정 설계가 작동했다.

---

## 3. 패킷 결함 역반영 목록 — **이 리포트의 본체**

### 3.1 1회차 지적 → 전건 반영 완료

| # | 지적 | 판정 | 조치 | 재확인 |
|---|---|---|---|---|
| 1 | 안전 규칙이 §9·§11 에 중복 | **생성기 버그** — §9.2 인라인분을 `seen` 에 등록 안 함 | `packet-gen.mjs` `seen.add` 추가 | 인라인 1회로 감소 |
| 2 | A3 가 차단 경로를 검증 못 함 | **수용 기준 결함** | A3 를 왕복 재현으로 교체 | §1.2 양쪽 통과 |
| 3 | 1,307줄인데 부록 대부분 무관 | **설계 결함** — 폐포가 하중/참고 절을 섞음 | 부록 2단 분리(§12 A · §13 B) | 감사 잔여 0 유지 |
| 4 | 검사 범위·표출 판별·훅 정책 없음 | **계약 공백** | HANDOFF → Fable `27010a1` 이 E9 §2 에 판별 규칙 3줄 신설 | **§1.3 양쪽 수렴 확인** |
| 5 | 패킷 §10.1 과 실험 지시 충돌 | 실험 프로토콜 결함 | `protocol.md` §2 예외 명문화 | — |

### 3.2 2회차 신규 지적 — 미결

양쪽이 **독립적으로 같은 지점**을 짚었다. 둘 다 짚었다는 것 자체가 실결함의 신호다.

| # | 지적 | 짚은 쪽 | 판정 | 조치 |
|---|---|---|---|---|
| 6 | **훅 "자기 산출" 식별 규약 없음** — 마커·헤더·해시 중 무엇인지 미정 | 양쪽 | **계약 공백**. Codex 는 `# virgil-contract-lint:managed-v1`, Grok 은 `managed-by: tools/install-hooks.mjs` 를 임의 채택 — 서로 다른 훅을 남의 것으로 오인한다 | HANDOFF 등재 (아래) |
| 7 | **`--staged` 수집 의미론 미정** — 파일 전체인지 변경 줄만인지, rename/delete 취급 | 양쪽 | 계약 공백. 기존 위반이 있는 파일의 무관한 변경까지 차단될 수 있다 | HANDOFF 등재 |
| 8 | **atmosphere 허용 경로에 `atmo/` 포함 여부** | Grok | 계약 공백. 포함 안 하면 분권 광원이 전부 막히고, 포함하면 분권 파일의 직접 생성이 통과한다 | HANDOFF 등재 |
| 9 | 위반 출력 포맷 미정 | 양쪽 | 경미 — 사람이 읽는 용도라 파싱 계약이 없으면 무해. 다만 CI 가 파싱하면 깨진다 | 보류(우선순위 낮음) |
| 10 | 주석·문자열 속 패턴 취급 미정(display-name 외) | 양쪽 | 계약 공백. 블록 주석 안 `new THREE…` 오탐 / 주석 우회 미탐 | HANDOFF 등재 |
| 11 | 부록 B 노이즈가 핵심 3줄을 묻는다 | Grok | **설계 지적** — §13 B 분리로 완화했으나 여전히 분량이 크다. 티켓 타입별 부록 필터가 다음 개선 후보 | 백로그 |

**규칙**: 지시서를 손으로 고치지 않는다. `data/manifest/*.json` 또는 `tools/packet-gen.mjs` 를
고치고 `packet-gen --all` 로 다시 찍는다. 계약 공백은 매니페스트가 아니라 계약 문서가 닫는다.

---

## 4. 판정

| 조건 | 충족 |
|---|---|
| 1. 기계형 ①에서 **전 에이전트**가 구조·게이트·프로세스 3축 통과 | **충족** — Codex·Grok 양쪽 |
| 2. 창작형 ②에서 구조·프로세스 통과 | **측정 완료 (2026-08-06)** — 계약 기재 축(소유·모듈 계약·발화 금지)·게이트·프로세스 전건 양쪽 수렴. 계약 미기재 축 1건(구독 집합)만 갈림 → §3 규칙대로 패킷/계약 결함 판정(#30). 품질 발산은 정상 범위 |
| 3. 리포트 결함 목록 전건 역반영 | 1·2회차 9/9 완료 · 실발주분(#12~29) 즉시 조치 가능분 전건 완료 · 파일럿 ②분(#30~34) 계약 소유자 결박 3건 HANDOFF 등재 |

**판정: 기계형 위임 개시 — 이미 가동, P0 실증 완료. 창작형 위임 — 조건부 개시 가능:
갈림이 전부 계약 공백으로 소급되므로(#30 보강 전제), 보강 후 T-P1-02 실발주 회수가 곧
재검증이다.** 계약이 정한 축은 창작형에서도 수렴한다는 것이 이번 측정의 핵심 소득 —
§1.3(기계형)과 같은 패턴이 발산형 과제에서 재현됐다.

## 6. 품질 이터레이션 실험 (후속 — 2026-08-06~07, 종결)

파일럿 ② 이후 "품질이 계약으로 이전되는가"를 별도 실험으로 쟀다 — 계약·기록:
[quality-iteration.md](quality-iteration.md) · 채점기: [grader-rig.md](grader-rig.md)
(독립 리뷰 [r0-review.md](r0-review.md) 20건 반영판).

**결과 요약**: 기준선 20/40 → R1 21 → R2 20 — 정체 2회 연속으로 규칙 종결.
**이산 항목은 계약으로 이전된다**(열거분 전건 이행 — 구조·프로세스 축은 3라운드 완벽).
**게슈탈트 품질(유기적 융합·비례·조형)은 이 구성으로 이전되지 않는다** — 수정이 새
프리미티브 노출을 낳는 두더지잡기 재현, D4 실격 3라운드 유지. 자체 채점 관대 편향은
증거 좌표 규율로도 안 닫힘(블라인드 채점 층이 필수). #30 폐집합 답신 반영 후 R2 가
room:changed+game:pause 를 정확히 열거 구독한 것으로 **파일럿 ② 잔여 재검증은 충족**
— 창작형 구조 축은 계약 보강으로 수렴함이 확인됐다.

측정된 것을 그대로 말하면 — **패킷 1장으로 서로 다른 두 외부 에이전트가 질문 0·소유 밖
편집 0 으로 같은 구조를 만들고 게이트 3건을 통과했다.** 검사 범위·표출 판별법·훅 공존이
계약에 명시되자 1회차에서 갈렸던 지점이 양쪽 일치로 수렴했다. 위임 시스템의 핵심 가정
("어떤 에이전트가 봐도 같은 구조가 나온다")이 기계형 티켓에서는 성립한다.

성립하지 않은 것도 분명하다. **창작형은 한 번도 재보지 않았고**, 2회차에서 양쪽이 함께
짚은 계약 공백 4건이 남아 있다. 특히 #6(훅 식별 규약)은 두 산출물이 실제로 다른 마커를
채택한 지점이라 — 지금 둘을 같은 트리에 넣으면 서로의 훅을 남의 것으로 오인한다.

**다음 순서**: (1) §3.2 의 계약 공백 4건을 HANDOFF 로 닫는다 → (2) ①의 산출 중 하나를
본체 P0 에 반영(채택 기준: 계약 준수 동률이므로 구현이 단순한 쪽 우선) → (3) 그 위에서
② 창작형 착수. P0 발주 자체는 지금 열어도 된다 — 기계형 조건이 충족됐다.

## 4.1 후속 — 캘리브레이션 산출물 머지 판정 (2026-08-06, P0 발주 세션)

위 (2)의 머지를 집행하기 전에 신규 계약(E9 §2 확정분, `66f7566`)과 정적 대조한 결과
**2벌 모두 미달 — 머지 기각, T-P0-01 재발주**로 전환했다(v3 §4.1이 예고한 경로).
본체에 넣고 재는 대신 클론에서 판정한 이유: 마커가 틀린 훅이 A2/A4 실행으로 본체
`.git/hooks` 에 설치되는 오염을 피하기 위해서다.

| 항목 | Grok (386줄) | Codex (475줄) |
|---|---|---|
| 훅 마커 `# virgil-contract-lint managed` 고정 | **미달** — `managed-by: tools/install-hooks.mjs` (install-hooks.mjs:16) | **미달** — `# virgil-contract-lint:managed-v1` (§3.2 #6 실측) |
| `--staged` 수집 `--no-renames` | **미달** — `--diff-filter=ACM` 만 (lint-contract.mjs:83). rename 이 R 분류로 조용히 검사 누락 | 미대조 (마커 실격으로 종결) |
| 블록 주석 `/* */` 제거 후 검사 | **미달** — 줄 주석만 제거 (stripLineComment) | 미대조 (동일) |

두 산출물 다 **당시 계약으로는 통과**였다 — 미달 3건 전부 §3.2 에서 계약 공백으로
판정돼 그 뒤에 확정된 조항이다. 즉 이 기각은 산출물 결함이 아니라 계약 진화의 결과이고,
"계약이 정하면 수렴하고, 계약이 비면 갈린다"는 §1.3 의 측정과 같은 방향의 증거다.

**재발주 결과 (같은 날)**: 신규 패킷으로 T-P0-01 을 codex 에 재발주한 산출이 확정 조항
전건을 그대로 구현했다 — 마커 `# virgil-contract-lint managed` 정확 일치 ·
`--diff-filter=ACM --no-renames` · 블록 주석 상태기계 · atmo/materials 허용 경로.
1·2회차에서 갈렸던 마커 지점이 계약 확정 후 **문자 단위로 수렴**했다. A1~A4 클론·본체
양쪽 실측 통과, 본체 머지 완료. §3.2 #6·#7·#10 이 실발주에서 닫혔음이 확인됐다.

## 5. P0 실발주 1군 — 결함 신규 지적 (2026-08-06)

T-P0-01(재발주)·05·06 을 실발주 프롬프트(`prompt-order.md` — 추측 예외 제거·§10.1 유지)로
codex 에 병렬 발주, 3장 전부 회수·머지 완료. 프로세스: 질문 0 · 소유 밖 편집 0(3장 전부) ·
T-P0-06 은 §10.1 대로 CONTRACT_CHANGE_REQUEST 를 HANDOFF 에 반환 — **반환 경로의 첫 실전
작동**이다. 신규 결함 지적:

| # | 지적 | 짚은 쪽 | 판정 | 조치 |
|---|---|---|---|---|
| 12 | §10.1 의 HANDOFF 편집 요구가 상단 docs/** 열람 금지와 충돌 | **3/3 전원** | 투입 프롬프트 결함 | `prompt-order.md` 열람 금지에 하단 추가 예외 명문화 — **닫힘** |
| 13 | T-P0-06 A1 `--url-suffix` 가 shoot.mjs 에 실재하지 않음 + 샷 실패에도 exit 0 | codex | **수용 기준 결함** — 실행 불가 명령 | A1 을 실행 가능한 체커(`check-low-preset.mjs`)로 교체, low 실샷 게이트는 HANDOFF 로 하네스 소유자에게 — **닫힘(잔여 HANDOFF)** |
| 14 | T-P0-06 A3 기준선이 gitignore 라 클론에 없음 — 공급 절차 부재 | codex | 수용 기준 결함 | A3 expect 에 "본체 머지 시점 실행" 명시 — **닫힘** |
| 15 | P5 "상관 1.0" 의 통계량 미정의(phi 를 추측 채택) · --mutate 범위 미정 | codex | 수용 기준 서술 공백 | T-P0-05 acceptance 에 phi 계수·분산 0 실패 판정 명문화 — **닫힘** |
| 16 | 규칙명·출력 포맷 고정 계약 부재(후속 자동화 파싱 불가) | codex(01) | §3.2 #9 재지적 — 경미 | 보류 유지. CI 파싱 도입 시점에 계약화 |
| 17 | 부록 분량 과대(§12·§13)·§11.1/§11.2 샷 규약 중복 | 3장 공통 | §3.2 #11 재지적 — 설계 | 백로그 유지(티켓 타입별 부록 필터) |
| 18 | low 프리셋 전체 스키마 미명시(필수 9값 외 TAA·DOF 등 추측) | codex | 경미 — 소비 코드가 열람 허용 범위(src/**)라 실추측 폭 작음 | 보류. 산출 검증에서 폴백·기존 프리셋 무변경 확인됨 |
| 19 | A4 가 기존 관리 훅의 백업·복원 절차 없이 비관리 훅 덮어쓰기를 요구 | codex(01) | 경미 — 검증 순서로 회피 가능(본체 머지에서 A4 를 설치 전에 실행) | 보류 |

### 5.1 P0 2군 (T-P0-03·04, 같은 날) — 신규 지적

3·4 모두 프로세스 전건 준수(질문 0 · 소유 밖 편집 0 · 남의 빚 무수정 · HANDOFF 반환 활용).
04 는 픽셀 검증 기준선이 클론에 없자 **기준 커밋을 같은 하네스로 재촬영해 자체 기준선을
만들고 provenance 를 보고**했다 — 본체 머지에서 공식 동결 기준선으로 재검증해 PASS.

| # | 지적 | 짚은 쪽 | 판정 | 조치 |
|---|---|---|---|---|
| 20 | **훅 × 위상 머지 마찰**: T-P0-01 훅이 파일 전체를 검사하므로, T-P0-02 소관 위반이 남은 props.js 를 포함한 T-P0-04 머지 커밋이 차단된다. 티켓 위상(분할→청소)과 훅 도입 순서의 상호작용을 발사문이 예견 못 함 | 머지 세션 실측 | **프로세스 결함** | 04 를 props.js 제외 7파일로 부분 머지(1/2) → 02 를 04 클론에서 파생 발주 → 청소분과 props.js 를 2/2 로 일괄 커밋. 훅 동작 자체는 계약이 명시한 의도대로 |
| 21 | 전체 스캔형 수용 기준(A1/A5)이 "전건 통과" 헤더와 충돌 — 타 티켓 기존 위반 때문에 종료 코드가 티켓 판정과 어긋남 | codex(03·04) 각자 | 수용 기준 서술 결함 | 03·02 는 규칙/범위 한정으로 정밀화 완료. 근본 해소는 tools 범위 계약 답신 + 잔여 티켓 소화 후 전건 0 복원 |
| 22 | A4 grep 의 `--include=*.js` 가 zsh 에서 글롭 오류(따옴표 없음) | codex(03) | 수용 기준 이식성 결함 | 백로그 — 매니페스트 명령 인용 규약(POSIX 안전) 신설 후보 |
| 23 | 정본 ID(`deitch.S1`)와 기존 배터리 로컬 ID(`S1`) 호환 계약 부재 — 로더가 이중 ID 제공으로 자체 해소 | codex(03) | 계약 공백(경미) | 산출 구현이 사실상 계약을 확정 — ARCH 데이터 접근층 절에 반영은 계약 소유자 소관, HANDOFF 미등재(구현 정본으로 충분) |
| 24 | `room:changed` 구형 소비자가 GAMEPLAY·AUDIO·ATMOSPHERE 소유라 03 단독 완수 불가 | codex(03) | 티켓 범위 결함 | codex 가 HANDOFF 3건으로 반환 — 큐 반영 완료. 위임 시스템의 반환 경로가 범위 결함을 정상 흡수한 사례 |
| 25 | 분할 신규 파일의 명명·경계·재export 방식 미지정 — 구현자마다 구조가 달라질 수 있음 | codex(04) | 창작 여백 vs 구조 고정의 경계 사례 | 기록만. 파일럿 ②(창작형)가 재는 것이 정확히 이 축 — 구조 축 판정 기준을 §2 표에 유지 |
| 26 | §7 은 3샷을 강제하는데 공통 규약(§11)은 1~2샷 권고 — 충돌 | codex(04) | 생성기 중복 절 충돌 | 백로그 — §17 부록 필터와 함께 정리 |
| 27 | **T-P0-02 1차 산출 픽셀 회귀로 기각** — testbed 광원 3개를 practical() 경유로 바꾸자 렌더되지 않는 씬의 광원이 전역 광원 예산을 점유, 복도 조명이 재배치(+4.3/255 · ev 54.65→49.36). props·kit-mat 도 ±0.3 비중립. 파일 단위 이분(3샷)으로 격리 | 머지 세션 실측 | **산출 결함 + 수용 기준 결함의 결합** — A3(픽셀 대조)가 클론에서 실행 불가라 에이전트가 회귀를 미검증 반환. 04 는 자체 기준선을 만들었지만 02 는 안 만듦 — 절차가 개인 재량이었다 | A3 를 자체 기준선 왕복(stash→기준 재촬영→복원→대조)으로 교체·경고 실측 인라인. **재발주** |
| 28 | 본체 corridor 렌더가 8/4 동결 기준선과 이미 6.605/255 (26.5%) 괴리 — 이 세션 첫 머지(config.js 단독) 이전부터 존재 | 머지 세션 실측 | 기준선 관리 결함(세션 밖 유입) | HANDOFF 로 기준선 소유자에게 반환 — 갱신 누락인지 미발각 회귀인지 판정 필요 |
| 29 | **practical() 팩토리 사이드이펙트** — 호출만 해도 전역 `S.seed`·광원 풀·씬이 변해 "팩토리 경유 + 픽셀 무변화"가 현 인터페이스로 동시 충족 불가. 1차 기각(#27)의 진짜 원인 | codex(02 재발주) | **계약·인터페이스 공백** | HANDOFF 반환(비등록 생성 모드 또는 atmosphere 소유자 협업 필요). 광원 4건과 props.js 2/2 커밋은 답신 대기 |

### 5.2 파일럿 ② (T-P1-02, 같은 날) — 신규 지적

| # | 지적 | 짚은 쪽 | 판정 | 조치 |
|---|---|---|---|---|
| 30 | **이벤트 구독 폐집합 미명시** — §6 은 이름 발명만 금지, 열거 외 기존 이벤트 구독 허용 여부 미정. 두 에이전트가 갈린 유일한 구조 축. Grok 재요청 보고가 정확한 원인을 짚음: **부록(ARCH §5/E8 — chars 는 pause 시 update 스킵)과 §6 정본 목록(room:changed 한 줄)이 서로 어긋난다** — 부록을 따르면 구독 초과, 정본을 따르면 pause 미대응 | 실측(§2 표) + grok 추측 기록 | **계약 공백** — 폐집합 강제냐(어휘 통제 우선) 최소집합이냐(game:pause 같은 정당 확장 허용)는 ARCH §5 소유자 결정 사안 | HANDOFF 등재 |
| 31 | **grok 하네스 제약** — acceptEdits 에서 mkdir·npm 류 터미널 명령이 자동 취소돼 조기 종료 2회(stopReason: cancelled 실측). bypassPermissions/dontAsk 는 상위 분류기가 차단 | 머지 세션 실측 | 하네스 결함(패킷 무관) | 클론 사전 공급(디렉터리·node_modules 링크)으로 우회, 5회차 완주. 보고·커밋 단계 취소는 §1.4 보고 재요청으로 회수 |
| 32 | `interrogation-deitch` 샷이 심문 공간·앵커 없이 테스트베드를 촬영 — 이름·의도와 실행 상태 불일치 | codex·grok 공통 우회 | 샷 배선 결함 | HANDOFF 등재(샷리스트 소유자) |
| 33 | 네온 소품 기본 텍스트 `'HOTEL CECIL'` — 영문 구명 표출, 한글 전용 린트의 사각 | 육안 검수(샷) | **표출 계약 사각지대** | HANDOFF 등재(PROPS+계약 소유자). 파일럿이 없었으면 제출 영상에서 발견됐을 결함 |
| 34 | A1 실루엣 판정이 자동 기준 없는 주관 판정 · `perf.js` 소비 시그니처(팩토리 반환형·뼈 이름) 미정의 · 휴머노이드 4종 요구인데 게이트는 1종만 검사 | codex | 수용 기준·후속 계약 공백 | 실루엣 판정은 G-판정자 소관 유지(품질 축). 시그니처는 T-P1-03 발주 전 매니페스트 보강 필요 — 백로그 등재 |

**#27 후속 — 수용 기준 수정의 효과 실측**: 재발주 codex 는 신설된 자체 기준선 왕복(A3)으로
1차 광원 전환의 회귀(7.536/255)를 **반환 전에 스스로 검출·원복**했고, 전건 통과가 아니라는
이유로 커밋하지 않았으며, 결정 불가 지점을 §10.1 CONTRACT_CHANGE_REQUEST 로 반환했다.
1차 발주(미검증 반환)와 정확히 같은 과제에서 수용 기준 한 줄의 차이가 결과를 갈랐다 —
**게이트가 지시서 안에 있으면 에이전트가 자기 회귀를 잡는다**는 실측. 머지 가능분(kit-mat
재질 3건)만 회수했고 본체 픽셀 동일(0/255) 확인 후 커밋.

### 5.3 P1 1파 (T-P1-01·03, 2026-08-07) — 신규 지적

| # | 지적 | 짚은 쪽 | 판정 | 조치 |
|---|---|---|---|---|
| 35 | **저장소 전역 린트를 수용 기준으로 건 것** — `node tools/lint-contract.mjs` 는 전 파일을 스캔하는데 잔여 위반 15건이 전부 타 소유다. 배타 소유 규약 아래에서 티켓 담당자는 자기 코드가 완벽해도 이 항목을 통과시킬 수 없다. 01·03 두 에이전트가 독립적으로 같은 지적을 냈다 | codex(01·03 공통) | **수용 기준 결함** — 티켓이 통제할 수 없는 전역 상태를 완료 조건에 넣었다. T-P1-02 만 우연히 올바른 문구("전체 스캔 출력에서 `<자기 파일>` 행 0건")를 갖고 있었다 | 01·03·06·07 매니페스트의 lint 항목을 02 문구로 교체 — 자기 소유 파일 행 0건이 통과이고 전역 종료 코드 1은 허용, 잔여는 HANDOFF 등재. 패킷 재생성 완료 |
| 36 | **§1 "전건 통과"와 §8 "도구 부재 시 통합 대기"가 완료 판정을 서로 다르게 정의** — playthrough 미존재 항목을 건너뛴 산출이 §1 기준으로는 미완료다. 01·03 두 에이전트가 같은 지적 | codex(01·03 공통) | **패킷 내부 모순** — 두 문장이 같은 문서 안에서 충돌한다 | 회수 판정은 §8 우선으로 운용(1파 실적용). 통합 게이트에서 유예 항목을 실행해 최종 판정 |
| 37 | **T-P1-03 §4.3(`game:pause` 구독 요구)과 §6 폐집합(`perf:state`·`deduction:link`만 허용)이 동시 충족 불가** — #30 의 재발. 매니페스트 events.listen 에 `game:pause` 가 빠져 있었다 | codex(03) | **매니페스트 누락** — 02·07·08 은 이미 game:pause 를 열거하고 있어 03 만 빠진 개별 결함 | 매니페스트에 `game:pause` 추가·패킷 재생성. **잔여**: 머지된 `perf.js` 는 아직 미구독 — 실제 pause 발화는 T-P1-05 산출이라 통합 게이트에서 검증(HANDOFF 등재) |
| 38 | **`rig.js`·`perf.js` 가 둘 다 order 30** — 동일 order 의 초기화·update 순서 계약이 ARCH §3 에 없다. 정렬은 안정 정렬이라 glob 경로 순(perf→rig)에 의존한다 | codex(03) | **계약 공백 — 현재는 무해**. 머지 후 실측: `rig.js` 의 update 는 `if (this.paused) return` 뿐이라 포즈를 쓰지 않는다. 관절을 쓰는 모듈이 perf 하나뿐이라 충돌이 없다 | 코드 변경 없음. 두 번째 관절 기록자가 생기는 시점에 ARCH §3 소유자가 동일-order 규칙을 정해야 한다 — HANDOFF 등재 |
| 39 | 티켓 제목·A1 은 다이치 4클립 범위인데 패킷 본문은 루이즈·프라이스·도일까지 전 인물 연기 계약을 싣는다 — 후속 티켓과의 구현 경계 미확정. 지시서가 1,711줄까지 커져 정본·참고 우선순위 판독 비용이 크다 | codex(03) | **패킷 비대화** — consumes 자동 인라인이 범위 밖 계약까지 끌어온다. 실제 산출은 경계를 지켰으므로 이번엔 무해 | 3파 발주 전 `consumes` 를 실소비 기준으로 정밀화할 때 함께 처리 |
| 40 | 로비 상호작용 7종과 A2 의 `qaId` 7종이 서로 다른 집합 — 소파·압지의 QA API 노출 여부 미명시. `pressure-log` 는 심문 grant 인데 "증거 4종 상호작용"에 묶여 레벨 배치 여부가 모호 | codex(01) | **수용 기준 모호** — 에이전트가 스스로 결정(§2 기록)해 진행했다 | 통합 게이트의 playthrough 실행에서 판정. 봇이 소파·압지를 요구하면 T-P1-06 회수 시 매니페스트 보강 |

**1파 회수 실측**: 01·03 모두 감사 청정(소유 파일 1개만 커밋 · 소유 밖 status 0 ·
`Math.random`/`Date.now` 0). 클론 재실행이 보고 수치와 일치했고(01 3샷 gate ok · 03 factcheck
전건 PASS), 본체 머지 후 재검증도 동일 — 3샷 gate ok · 콘솔 0 · `performance` 모듈 자동 등록.
**두 에이전트가 독립적으로 같은 수용 기준 결함(#35·#36)을 짚었다** — 지적이 겹친다는 것은
그것이 개별 오독이 아니라 지시서 자체의 결함이라는 신호다.

### 5.4 P1 1파 T-P1-05 — 신규 지적

| # | 지적 | 짚은 쪽 | 판정 | 조치 |
|---|---|---|---|---|
| 41 | **§2 소유 파일에 `src/core/shotlist.js` 가 없는데 §7 은 수정을 의무화** — §9.2 예외 조항을 찾아야만 해소되는 충돌 | codex(05) | **패킷 결함** — core 잠금 예외가 소유 목록에 반영되지 않았다 | 매니페스트 `owner_files` 에 shotlist.js 를 예외 표기(scope: entries)로 등재 — 06·07·10 발주 전 반영 |
| 42 | **§10.2 는 A1~A4 실제 출력을 모두 요구하는데 §8 은 A2~A4 를 건너뛸 수 있다고 한다** — #36 의 세 번째 발현. 건너뛴 항목을 통과·면제·대기 중 무엇으로 보고할지 미정 | codex(05) | 패킷 내부 모순 — 1파 3장 전부가 같은 지적을 냈다 | §8 문구에 "건너뛴 항목은 **대기**로 보고한다(통과도 면제도 아니다)"를 명시 — 2파 발주 전 반영 |
| 43 | **감도 계약이 `player.js` 의 SENS 인스턴스 필드를 전제하는데 실제 선행 트리에는 상수 클로저뿐** — 선행 의존 검증 항목 부재 | codex(05) | **선행 계약 드리프트** — 패킷이 존재하지 않는 인터페이스를 계약으로 인용했다 | HANDOFF 로 GAMEPLAY 반환. **통합 게이트의 `--assert-settings` 감도 항목이 이 결박에 걸린다** — 2파 이후 우선 해소 |
| 44 | 저장 보유 판별 키 미지정 · 프레임 "메모리" 단위(객체 수/바이트) 미정의 · D7 을 "기계 판정"이라 부르지만 시스템 폰트·기본 버튼·라운드 코너·이모지를 검사하는 자동 검사가 샷 하네스에 없다 | codex(05) | **수용 기준 공백 3건** — 특히 D7 은 판정 수단 없이 기계 판정을 선언했다 | 저장 키·메모리 단위는 T-P1-06 매니페스트에 명시. D7 자동 검사는 T-P1-10(qa 하네스) 범위로 이관 — 그때까지는 육안 검수가 판정자다 |
| 45 | T-P0-01 을 완료 의존으로 선언했지만 **pre-commit 훅 설치 여부·기존 main.js staged 린트 적합성을 검증하는 착수 조건이 없다** — 실제로 main.js 를 건드리는 순간 기존 위반 2건이 드러났다 | codex(05) | **의존 선언의 공백** — "T-P0-01 done" 이 훅이 실제로 설치·통과 가능함을 뜻하지 않았다 | 회수 시 발주 세션이 `[virgil]` 교체로 해소(#29 답신 권고 준용). 매니페스트 depends 에 "착수 시 `node tools/lint-contract.mjs --staged` 가 자기 수정 대상 파일에서 통과함을 확인" 조항 추가 |

**1파 3장 공통 지적의 의미**: `#36`(전건 통과 vs 건너뛰기)은 01·03·05 **세 에이전트가 전부**
독립적으로 짚었고, `#35`(전역 린트)는 01·03 이 짚었다. 서로 다른 클론에서 같은 문장을 문제로
지목했다는 것은 개별 오독이 아니라 **지시서 템플릿의 결함**이라는 뜻이다 — 개별 티켓이 아니라
`tools/packet-gen.mjs` 의 §8·§10.2 템플릿을 고쳐야 재발이 멎는다.

### 5.5 P1 2파 T-P1-04 — §10.1 반환 (코드 0줄, 계약 결함 5건 적발)

T-P1-04 는 **구현을 시작하지 않고 반환**했다. 수용 기준 A1~A3 는 전건 실행·통과시키고
(배터리 1098 assertions · factcheck 전 게이트), A4 는 규약대로 대기 처리한 뒤, 계약이
서로 충돌해 착수가 불가함을 §10.1 로 반환했다. 지적 5건이 전부 실재로 확인됐다.

| # | 지적 | 검증 | 판정 | 조치 |
|---|---|---|---|---|
| 46 | **`act:enter` 발신자 이중 정의** — §5 는 "발화 파일: interrogation.js" 인데 잠긴 `src/core/state.js:24` 의 `setAct()` 가 이미 발화한다 | 실측 확인 — state.js:24 에 `this.bus.emit('act:enter', …)` 존재 | **계약 충돌** — 계약대로 하면 이중 발신, core 를 따르면 계약 위반. 착수 불가가 맞다 | ARCH 소유자 반환 + 재발주 forbidden 에 "직접 emit 금지 · `state.setAct()` 호출" 결박 |
| 47 | **A2 가 담당자 소유 밖 파일의 동작을 요구** — `--burn` 은 `tools/test-interrogation.mjs` 의 인자인데 이 파일이 `owner_files` 에 없다. 게다가 현 배터리는 argv 를 읽지 않아 `--burn` 실행이 기본 실행과 출력이 완전히 같다 | 실측 확인 — 배터리에 argv 처리 0 | **수용 기준 결함** — 배타 소유 규약 아래 구조적으로 통과 불가. #35 와 같은 계열(통제 밖 대상을 완료 조건에 넣음) | 배터리를 `owner_files` 에 추가 |
| 48 | **A2 의 "역직렬화 후에도 유지" 가 달성 불가** — `state.serialize()` 는 있지만 restore 경로가 없고 `src/gameplay/save.js` 도 없다. core 는 잠김 | 실측 확인 — state.js 에 serialize 만 존재, deserialize 없음 | **수용 기준 결함** | 직렬화까지만 판정하고 역직렬화는 대기로 명시 |
| 49 | **실코드가 쓰는 이벤트 3종이 ARCH §5 미등재** — `qa:state`(main.js 발화 · 5개 모듈 구독) · `interrogation:evidencePicked`·`interrogation:needEvidence`(interrogation.js:134,221 구현, 배터리가 판정에 사용). 폐집합 규칙 아래에서는 기존 배터리 통과와 계약 준수가 양립 불가 | 실측 확인 — grep 으로 6개 파일 사용 확인, §5 표에 0건 | **문서 지연** — 설계 결정이 아니라 §5 표가 실코드보다 뒤처진 것 | ARCH 소유자 반환. `manifest-check` R4 를 고쳐 **src/** 가 실제로 emit/on 하는 이름은 오류 대신 `[DRIFT]` 출력**으로 바꿨다 — 문서 지연이 발주를 막지 않되 미등재가 매 실행 보인다 |
| 50 | `evidence:granted`·`flag:set` 이 매니페스트 emit 목록 누락 · 막 전환에 필요한 `player:interact` 가 listen 누락 · "판정표 6행" 이 실제 결과 7가지와 불일치 | ARCH §5 v2.3·E5 [구현] 대조 | 매니페스트 누락 3건 | 전건 역반영 |

**이 반환의 값어치**: 다섯 건 모두 착수했다면 잘못 구현됐거나 중간에 막혔을 것이다.
`#47`·`#48` 은 **구조적으로 통과 불가한 수용 기준**이라 아무리 잘 구현해도 완료 판정이
나지 않았고, `#46`·`#49` 는 어느 쪽을 따라도 계약 위반이 되는 이중 구속이었다.
1파에서 신설한 §8 "착수 전 확인"과 §10.1 반환 경로가 **코드를 쓰기 전에** 이것들을
잡아냈다 — 지시서 게이트가 구현 낭비를 앞에서 막은 첫 사례다.

**드리프트 판별을 손목록이 아니라 실코드로 둔 이유**: 예외를 사람이 적으면 그 목록 자체가
다음 세션의 거짓말이 된다. `manifest-check` 는 `src/**` 를 스캔해 그 이름으로 실제
emit/on 하는지 보고, 그럴 때만 DRIFT 로 통과시킨다. ARCH 에 등재되는 순간 목록에서
자동으로 빠지므로 청소를 잊을 수 없다.

### 5.6 통합 회귀 — 단독 통과 3장이 합쳐지자 깨졌다 (2026-08-07 회수 세션 실측)

| # | 지적 | 짚은 쪽 | 판정 | 조치 |
|---|---|---|---|---|
| 51 | **`atmo-rooftop-rain` 에 검은 폴리곤 블록 다수** — 물탱크·하늘·배관을 가리는 파탄급 아티팩트. 게이트는 `gate ok` 로 통과했다(밝기·클리핑 축만 봄). 육안 검수에서만 발견 | 회수 세션 육안 검수 | **통합 회귀 — 단일 티켓 결함 아님**. 이분 결과: `lobby.js`(T-P1-01) 단독 정상 · `perf.js`(T-P1-03) 단독 정상 · `settings.js`(T-P1-05) 단독 정상 · lobby+perf 정상 · **lobby+settings 에서만 발현**. 원인은 `settings.js` 가 init 에서 기본 FOV(70)를 카메라에 적용하는 것 — QA 모드에선 샷 하네스가 카메라를 소유(shotlist fov 38)하는데, 파이프라인이 첫 프레임에 잡아두는 투영 의존 상태가 그 뒤 바뀐 샷 카메라와 어긋난다. lobby 가 있어야 어긋남이 픽셀로 드러난다 | `_apply('fov')` 에 `if (this.engine.qa) return` 가드 추가 — 같은 모듈의 품질 프리셋이 이미 쓰는 `!engine.qa` 가드와 동일 근거. 실측 `dark 4.4% → 0.11%`, 아티팩트 소멸 |
| 52 | **`dark%` 가 이 아티팩트의 값싼 탐지기였다** — 정상 0.11~0.12% vs 파탄 4.4%. 그런데 게이트 판정은 `dark` 를 임계로 쓰지 않아 `gate ok` 가 나왔다 | 이분 과정에서 발견 | **게이트 결함** — 검은 블록이 화면의 4.4% 를 덮어도 통과한다 | 샷별 `dark` 기준선을 두고 급변을 FAIL 로 올리는 것이 필요 — T-P1-10(qa 하네스) 범위로 HANDOFF 등재 |
| 53 | `corridor-long` 샷이 코리도가 아니라 벽 텍스처만 촬영 — 머지 이전 커밋(`b578121`)에서도 동일 재현 | 이분 대조 | **기존 결함**(#32 계열) — corridor 레벨이 아직 없어 테스트베드 벽을 찍는다. 이번 머지와 무관 | `shots/_baseline/corridor.png` 도 이 샷과 대응하지 않는다 — 기준선 소유자 반환(#28 미해소분에 병합) |

**이 발견의 방법론적 의미**: 세 티켓이 각자의 클론에서 전건 통과했고 회수 감사도 청정했다.
그런데 **합치자 깨졌다** — 어느 티켓의 잘못도 아니고, 어느 티켓의 수용 기준으로도 잡히지
않는 결함이다. 배타 소유·클론 격리 발주 구조는 이 계열의 결함을 원리적으로 못 잡는다.
잡은 것은 **머지 후 본체 재촬영 + PNG 육안 검수**뿐이었고, 기계 게이트(`gate ok`)는
통과시켰다. CLAUDE.md 의 "코드만 읽고 품질을 판정하지 않는다 — PNG 를 Read 로 직접 본다"
규약이 없었으면 제출물에 그대로 실렸다.

**이분 절차(재사용 가능)**: 각 티켓의 클론이 살아 있으면 `클론 단독 → 두 파일 조합 →
본체` 순으로 같은 샷을 찍어 `dark%` 를 비교한다. 파일을 임시로 옮겼다 되돌리는
(`mv src/ui/settings.js /tmp/…off`) 방식이 가장 빨랐다 — 모듈 자동 등록(glob)이라
파일 존재만으로 켜고 끌 수 있다.

#### 5.6 정정 — #51 의 귀속을 실측으로 다시 나눈다 (같은 날 후속)

§5.6 을 쓸 때는 검은 블록을 통째로 "통합 회귀"로 귀속했다. **그 귀속은 절반만 맞다.**
추가 이분으로 **두 개의 별개 현상**임이 드러났다.

| 현상 | 재현 조건 | dark% | 원인 | 귀속 |
|---|---|---|---|---|
| (가) 단발 촬영 아티팩트 | `atmo-rooftop-rain` 단독 | 정상 0.11% → **4.4%** | `settings.js` 가 init 에서 기본 FOV 를 적용, QA 모드에서 샷 하네스의 카메라와 어긋남 | **통합 회귀 맞음** — lobby+settings 조합에서만 발현. `engine.qa` 가드로 **해소** |
| (나) 시퀀스 아티팩트 | 앞선 샷이 `engine.time` 을 ~50 이상으로 민 뒤의 `atmo-rooftop-rain` (최소 재현 6샷: `deduction-board` → atmo 4종 → rooftop) | 0.11% → **0.8%** | 미상 — 시간 의존 | **기존 결함. 머지 무관** |

**(나)의 귀속 근거(실측)**: `settings.js` 를 꺼도 0.8%, `lobby.js` 를 꺼도 0.8%,
`perf.js`·`cinematics.js`·`title.js` 를 꺼도 0.8%. 그리고 결정적으로 **머지 이전 커밋
`b578121` 클론에서 같은 6샷 시퀀스를 돌리면 같은 위치에 같은 검은 블록이 0.8% 로 재현된다**
(그 클론에는 lobby·perf·settings·title·cinematics 가 아예 없다).

**기전 단서**: `engine.js:127` 의 `advanceTo` 가 `Math.max(t, self.time)` 라 **샷의 선언된
`time` 은 현재 시간보다 클 때만 존중된다**. 전 스위트에서 rooftop 은 선언값 27 이 아니라
t≈55 에 촬영된다. 단발·짧은 시퀀스는 t 가 작아 깨끗하고, 앞 샷이 t 를 50 이상으로 밀면
터진다. 시간이 큰 값일 때 발산하는 애니메이션·플리커 항이 원인일 가능성이 높다.

**왜 이 정정이 중요한가**: 이 문서는 제출물 4번의 재료다. "머지가 회귀를 만들었다"를
정정 없이 두면 **실제로 고친 것(가)과 원래 있던 것(나)이 뭉개져** 위임 시스템의 실적이
왜곡된다. 회수 세션이 만든 회귀는 (가) 하나고 그건 닫혔다. (나)는 8/4 이전부터 있던
것이며 소유자 반환 대상이다.

**절차 교훈**: 이분에서 "A 를 끄니 정상"을 원인 확정으로 쓰면 안 된다. (가)를 잡을 때
`settings.js` 를 끄니 정상이었지만, 그건 그 시점 조건(단발 촬영)에서만 참이었다.
**머지 이전 기준선에서 같은 재현 절차를 돌리는 대조군**이 있어야 회귀와 기존 결함이
갈린다 — 이번에 그 대조를 뒤늦게 돌려 귀속이 뒤집혔다.

### 5.7 P1 마지막 파 T-P1-06 — 두 번째 §10.1 반환 (의존 그래프·수용 기준 공허성)

| # | 지적 | 판정 | 조치 |
|---|---|---|---|
| 54 | **완주 봇이 심문을 끝낼 수 없다** — `qa.choose` 는 T-P1-09 산출인데 T-P1-06 의 depends 는 `[01, 04]` 였다. 클론에 `__CECIL__.qa.choose` 가 없어 A1 의 1막 완주가 원리적으로 불가 | **의존 그래프 오류 — 회수 세션 책임**. 2파 구성 때 depends 를 "실소비 기준으로 정밀화"하면서 04 만 보고 09·10 을 놓쳤다 | depends 를 `[01, 04, 09, 10]` 으로 교정. 06 은 09 머지 후 재발주 |
| 55 | **관찰형 증거(keyrack·flask)의 gaze 시간이 진행되지 않는다** — T-P1-10 의 qa 하네스가 `interact` 는 구현했으나 응시 누적 경로를 구동하지 않아 증거 4종 획득이 불가 | **선행 산출의 기능 공백** — 10 은 자기 수용 기준을 통과했지만(전부 playthrough 대기라 클론 내 검증 0건) 소비자가 쓸 수 없었다. #44 가 경고한 "판정 수단 없는 완료"의 실제 발현 | HANDOFF 로 GAMEPLAY 반환. 06 재발주 전 해소 필요 |
| 56 | **"전체 스캔에 자기 파일 행 0건" 이 신규 산출 티켓에서 공허하다** — 파일을 아예 만들지 않아도 문언상 통과한다 | **회수 세션이 오늘 만든 결함**. #35 를 고치며 도입한 문구가 신규 파일 케이스를 빠뜨렸다 | T-P1-06 수용 기준을 `test -f tools/playthrough.mjs && node tools/lint-contract.mjs` 로 교체. `packet-gen` §8 에 신규 산출 티켓 경고 절 신설 — 같은 문구를 쓰는 다른 티켓(P0-03·P1-01·02·03·07)은 전부 파일을 실제로 산출해 물리지 않았다 |

**반환 2회의 손익**: T-P1-04·06 두 티켓이 코드를 쓰지 않고 반환했고, 두 번 다 지적이
전건 실재했다. 발주 비용은 들었지만 **잘못 구현될 뻔한 것을 앞에서 막았다** — 특히 #54 는
착수했다면 봇이 심문 구간에서 무한 대기하거나 가짜 통과 경로를 만들었을 것이다.
`#56` 은 회수 세션이 오늘 만든 결함을 외부 에이전트가 되짚어 준 사례다 —
**게이트를 고치는 쪽도 검증 대상**이라는 뜻이다.

### 5.8 배터리 축소 건 — 독립 검토 판정과 채택 결론 (2026-08-07)

디렉터 지시로 **독립 codex 세션**(read-only 클론)에 판정을 맡겼다. 회수 세션 진단에
**부분 반대**가 돌아왔고, 반대 지점이 옳아 채택했다.

| 쟁점 | 회수 세션 주장 | 독립 검토 판정 | 채택 |
|---|---|---|---|
| factcheck S0 와 구 스윕의 중복 | "데이터 원천이 달라 **전혀** 겹치지 않는 순수 손실" | **과장.** `script.js` 의 truth·correct·evidence 는 `case-graph.json` 에서 hydrate 되므로 `refutedBy→evidence` 존재성은 일부 겹친다. 고유 손실은 **graph→hydrated script→수동 EVIDENCE 레지스트리 경계**의 `grants`/`spawns`/variant 교차 검증 | **검토 채택.** 표현을 "부분 중복 + 경계 미검증"으로 정정 |
| 축소의 원인 | 데이터 스윕 소실 | **부정확.** 구 배터리는 전 인물·전 진술·전 선택 **매트릭스**도 돌렸다. 현 배터리는 다이치 중심 선별 경로다 | 검토 채택. 회수 세션 실측이 이를 뒷받침 — 구 배터리를 새 구현에 돌리면 **1041 passed / 31 failed**(매트릭스는 여전히 돈다) |
| 런타임 위험 | 명시 안 함 | **예외가 아니라 조용한 기능 손실.** 깨진 grant 는 검증 없이 발화되고, 소비자는 정의 없으면 `false` 로 조용히 거절한다. 하드 진행 불능보다 증거·분기 품질 저하 | 채택 |
| 근본 원인 | "수용 기준에 **단언 수 하한**이 없었다" | **방향만 맞다.** 1098개 중복 단언보다 20개 정확한 불변조건이 강할 수 있어 수량은 계약 지표가 못 된다. 진짜 원인은 **구현자에게 구현과 가변 acceptance oracle 을 함께 소유시킨 것** | **검토 채택.** 회수 세션 진단을 폐기하고 이쪽으로 교체 |
| 조치 | 복원(B) 또는 재발주(C) | **D안** — 데이터 동결 + 제출 전 전수 검사로 스윕을 대체하고, 남은 예산은 **더 시급한 결함**에 쓰라 | 채택 |

**독립 검토가 찾은 더 시급한 결함(#57)**: `evidence:granted` 를 구독하는 코드가 저장소에
없다. 회수 세션 실측으로 확인 — 발화 후 `state.has('pressure-log') = false`. 심문으로 얻은
증거가 인벤토리에 들어오지 않아 루이즈 반박 경로가 닫힌다. **배터리 64/0 · factcheck 전 게이트 ·
전 샷 28/28 · 콘솔 0 이 모두 통과하는 상태에서 핵심 루프가 끊겨 있었다.** T-P1-11 로 티켓화.

**검토를 그대로 받지 않은 지점**: 검토는 같은 이유로 `flag:set` 도 위험으로 묶었다. 실측하면
`flag:set {doyle-named}` 발화 시 `state.is('doyle-named') = true` 다 — 플래그는 상태기계가
내부에서 세우고 이벤트는 ARCH §5 규정대로 통지 전용이라 무관하다. **독립 검토도 검증 대상이다.**

**D안 실행 — 배터리 스윕의 대체 보증**
1. 구 스윕을 현재 데이터에 재현 실행: **95 passed / 0 failed**(회수 세션 실측). 지금 깨진 참조는 없다.
2. 그 교차 검증을 `tools/test-unlocks.mjs` U5 로 **상시 게이트에 복원**했다 — 발주 세션 소유라
   구현 티켓이 약화시킬 수 없다. D안의 "제출 전 전수 검사"를 일회성 기록이 아니라 매 실행
   게이트로 올린 것이다(검토 권고보다 강하게).
3. narrative 데이터(`case-graph.json`·`script.js`)는 리허설(8/8)까지 **동결**한다. 변경이
   필요하면 U5 재통과가 전제다.

**위임 계약에 추가할 조항 (독립 검토 제안 채택)**
- 기존 테스트 파일은 기본 append-only. 삭제·대체에는 제거된 불변조건 목록과 대체 게이트 명시 필수.
- 커버리지는 **단언 수가 아니라 의미**로 명시한다 — "전 인물×전 진술×판정", "교차 원천 참조 무결성".
- production 과 acceptance oracle 을 **함께 소유시키지 않는다**. 불가피하면 독립 회수자가
  테스트 diff 와 변이 검사를 승인한다. **T-P1-11 은 이 조항을 처음 적용한 티켓이다** —
  게이트(`test-unlocks.mjs`)를 발주 세션이 착수 전에 쓰고, 편집을 §9 금지로 결박했다.
- 이벤트 기반 기능은 발화가 아니라 **소비자 수신 → 상태 변화**까지를 단일 E2E 기준으로 건다.
- 핵심 E2E 가 대기면 티켓 상태는 `done` 이 아니라 `pending-integration`.

### 5.9 게이트 선작성 패턴의 실적 — T-P1-11 과 T-P1-06 재발주 (2026-08-07)

독립 검토가 지목한 근본 원인(**구현자가 자기 acceptance oracle 을 함께 소유하면 구현을
약화해도 같은 명령이 통과한다**)의 교정을 두 티켓에 적용했다. 절차는 같다 —
**회수 세션이 착수 전에 실패하는 게이트를 쓰고, 게이트 편집을 §9 금지로 결박한 뒤 발주한다.**

| 티켓 | 게이트 | 착수 전 실측 | 결과 |
|---|---|---|---|
| T-P1-11 unlocks 소비 배선 | `tools/test-unlocks.mjs` U1·U2 | 6 passed / **2 failed** | 8/0 통과 · **게이트 파일 diff 0**(무변조 확인) · `evidence.js` 3줄만 수정 |
| T-P1-06 완주 봇 재발주 | 같은 파일 U6 | 8 passed / **1 failed** | 발주 시점 |

**#58 관찰형 증거가 QA 에서 원리적으로 도달 불가**: `evidence._onInteract` 는
`mode === 'observe'` 를 명시적으로 거절하고("관찰형은 집히지 않는다"), 실제 획득은
`onFocus` → `gaze` 누적으로만 일어난다. QA 모드는 player 의 조준 레이캐스트를 돌리지
않으므로 `qa.interact` 만으로는 observe 증거가 잡히지 않는다. 해당 증거는
keyrack·flask·footprints·sink-trap·hatch-lock·wrench·shoes **7종 — 전 증거 14종의 절반**이다.
T-P1-06 이 §10.1 로 짚었고 실코드로 확인했다. 닫지 않으면 재발주해도 같은 지점에서 또 반환된다.

**조치**: T-P1-06 에 `src/gameplay/player.js` 의 **qa 관찰형 진입점 절**을 공동 소유로 추가하고
U6 를 첫 수용 기준으로 걸었다. 별도 티켓을 세우지 않은 이유는 두 가지다 — ①진입점의
형태(`qa.observe(id)` 신설이냐 `_qaInteract` 의 observe 분기냐)는 **소비자인 봇 작성자가
가장 잘 안다** ②마감·예산 제약에서 발주 1회로 묶는 편이 낫다. 소유가 겹치는 T-P1-10 은
이미 done 이라 동시 편집 충돌은 없다.

**이 패턴이 값싼 이유**: 게이트는 각각 20~30줄이고, 실패하는 상태로 먼저 만들어 두면
**발주가 곧 TDD 가 된다**. T-P1-11 의 보고에 "수정 전 6/2 → 수정 후 8/0" 이 자동으로 남았다 —
회수 세션이 요구하지 않아도 실패→통과 기록이 산출물에 붙는다.

### 5.10 P1 완주 — 통합 게이트 실측 (2026-08-08)

**T-P1-12 머지로 게임이 처음 1막을 스스로 완주한다.**

```text
node tools/playthrough.mjs --fast --act 1   → exit 0
  증거 4종 PASS: register, keyrack, flask, pressure-log
  괴담 접촉 PASS: lore:heard 3건
  1막 완주 PASS: act:enter{act:2} @ 1:10 (상한 14:42)
  무사건 간격 PASS: 최대 2:30 (기준 3:00 이하)
  콘솔 PASS: error 0 · warning 0
```

획득 증거 4종의 구성이 이 세션의 실적을 그대로 보여준다 — `pressure-log` 는 T-P1-11 이
닫은 `evidence:granted` 소비 배선이 **실제 플레이 경로에서** 작동한 결과이고(정적 게이트
U1·U2 가 아니라 봇이 확인했다), `keyrack`·`flask` 는 T-P1-06 이 닫은 관찰형 진입점(U6)의
결과다. `interrogation:start` 발화자(T-P1-12·U7)가 없으면 이 중 무엇도 도달하지 못한다.

| 게이트 | 결과 |
|---|---|
| 완주 봇 1막 (`--fast --act 1`) | **PASS** · exit 0 |
| 30초 캡처 (`--capture first30`) | **PASS** · 31프레임 · 5행 · 콘솔 error 0 warning 0 |
| 심문 배터리 | **64/0** |
| unlocks 반영 경로 | **10/0** (U1~U7 전건) |
| factcheck 전건(P4·P5·J1~J3·R1~R3) | **전 게이트 PASS** |
| `npm run shot` 전 샷 | **28/28** · bootErrors 0 · console 0 · gateFailures 0 |
| 계약 린트 | 잔여 6건 — **전부 타 소유 기존 위반**(HANDOFF 등재분) |

### 5.11 배선 공백 3연발 — 이 세션의 가장 큰 구조적 발견

같은 계열의 결함이 **세 번** 나왔고, 셋 다 **모든 게이트가 통과하는 상태에서** 숨어 있었다.

| # | 공백 | 발견 경로 | 결과 |
|---|---|---|---|
| 57 | `evidence:granted` **소비자 0** — 발화는 되는데 구독자가 없어 심문 증거가 인벤토리에 안 들어옴 | **독립 codex 검토**(디렉터 지시) | 사건을 풀 수 없었다 |
| 58 | qa 하네스에 **관찰형 증거 진입점 없음** — `_onInteract` 가 observe 를 명시적으로 거절하고 실제 획득은 조준 응시 누적으로만 일어나는데 QA 는 레이캐스트를 안 돌린다. 도달 불가 증거 7종 = 전 증거의 절반 | T-P1-06 §10.1 반환 | 봇이 증거를 못 모았다 |
| 59 | `interrogation:start` **발화자 0** — 구독자 셋(interrogation·atmosphere·audio)에 발화자 없음. ARCH §5 는 발신자를 gameplay 로 지정 | T-P1-06 재발주 A2 실패 | NPC 를 눌러도 심문이 시작되지 않았다 |

**공통 원인**: ①검증이 **발화만** 본다 — 티켓의 수용 기준이 "이벤트를 발화하는가"에서 멈추고
"소비자가 받아 상태가 바뀌는가"를 보지 않았다. ②**소유가 생산자에서 끊긴다** — 생산자
티켓은 자기 파일만 소유하므로 소비자 배선은 누구의 완료 조건도 아니었다.

**티켓 단위 검증으로는 원리적으로 못 잡는다.** 각 티켓이 자기 클론에서 전건 통과했고 회수
감사도 청정했다. 잡아낸 것은 셋 다 **경계를 넘어 보는 장치**였다 — 독립 검토(#57),
소비자 티켓의 반환(#58·#59). 그래서 계약에 넣은 조항이 이것이다:

> **이벤트 기반 기능은 발화가 아니라 소비자 수신 → 상태 변화까지를 단일 E2E 기준으로 건다.**

`tools/test-unlocks.mjs` 가 그 조항의 상설 집행기다. U1·U2(소비자 존재·핸들러 실효),
U3(단일 발신자), U6(QA 진입점), U7(발화자 존재)이 전부 **경계 검사**다. 발주 세션 소유라
구현 티켓이 약화시킬 수 없다.

### 5.12 `npm run shot` 28/28 을 그대로 읽으면 안 되는 이유 (게이트 판독 정정)

통합 게이트에서 전 샷이 `gate ok` 로 통과했다. **그 숫자를 성취로 읽으면 틀린다** —
육안 검수에서 두 계열의 문제가 드러났고, 둘 다 게이트는 통과시킨다.

**(가) 아직 없는 레벨을 찍는 샷 6개.** P1 이 지은 레벨은 로비 하나다(레벨 3종은 P2 — E10 §2).
그런데 샷리스트에는 코리도·942호·옥상 샷이 있고, 그 좌표에는 아무것도 없어 **카메라가 로비의
벽·천장을 찍는다**. 실측: `corridor-long` 은 벽지 벽면, `rooftop-ladder` 는 로비 천장과
샹들리에를 찍는다. 이 6개(`corridor-long`·`corridor-942`·`room942-bed`·`room942-bath`·
`rooftop-tanks`·`rooftop-ladder`)는 **현재 아무 의미도 판정하지 않는다**. #32·#53 과 같은 계열이며
`shots/_baseline/corridor.png` 도 이 샷과 대응하지 않아 `regress.mjs` 판정이 무의미하다.
→ **28/28 중 실질 판정은 22개**다.

**(나) 시간 의존 아티팩트 — dark% 는 이 계열의 탐지기가 아니다.** §5.6 정정에서 dark% 를
값싼 탐지기로 제안했는데 **밝은 장면에서만 맞다.** `atmo-interrogation` 실측:

| 조건 | t | dark% | 검은 블록 |
|---|---|---|---|
| 단독 촬영 | 31.8 | 5.13% | **없음** |
| 전 스위트 | 55.7 | 6.53% | **있음** |

어두운 심문실은 dark 화소가 원래 많아 오염 여부와 dark% 가 갈리지 않는다. 밝은
`atmo-rooftop-rain` 은 0.11% ↔ 0.8% 로 갈렸지만 그건 배경이 밝아 우연히 분리된 것이다.
**T-P1-10 에 반환한 "dark% 급변을 FAIL 로" 제안은 이 한계를 명시해 수정해야 한다** —
장면 밝기로 정규화하거나, 애초에 다른 지표(연결 성분 단위의 순흑 덩어리 검출)를 써야 한다.

**기전 확정**: `engine.js:127` 의 `advanceTo` 가 `Math.max(t, self.time)` 라 **샷이 선언한
`time` 은 현재 시간보다 클 때만 존중된다.** 전 스위트에서는 앞선 샷들이 시간을 밀어 올려
atmo 프로브가 선언값(23~31)이 아니라 t≈52~56 에 촬영된다. 단독·짧은 시퀀스가 깨끗하고
전 스위트만 오염되는 이유가 이것이다. **머지 이전 커밋 `b578121` 에서도 동일 재현되므로
기존 결함이다.**

**남은 조치(소유자 반환)**: ①샷 시간 결정성 — 샷마다 선언된 `time` 을 실제로 보장할지
(시간 되감기 또는 샷별 세션 분리) 정하라. 지금은 샷 순서가 결과를 바꾼다. ②없는 레벨의
샷 6개를 P2 착수 전까지 보류 표기하라 — 통과 수치가 실제보다 부풀어 보인다.
