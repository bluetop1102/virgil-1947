# 발사문 S6 — 타이틀 화면 (UI/TITLE)

> 실행자: Claude 새 세션. 제출 **8/10**.
> 발주 근거: 사용자 실플레이 체험 피드백 — **"첫 화면도 게임처럼 약간 웅장하고 멋있는 이미지 등으로
> 표현하면 될 것 같다"**. 심사자가 제일 먼저 보는 화면이고, 지금은 검은 배경에 금색 텍스트뿐이다.

## 0. 현황

`shots/base-r2/ui-title.png` 를 먼저 봐라(Read 도구로 직접). 순흑 배경 · 금색 세리프 "VIRGIL" ·
"1947 · ROOM 942" · 놋쇠 명패 버튼 2개("이어서"/"처음부터"). **디제틱하고 절제됐지만 화면이 비었다.**
정보는 다 있는데 "게임을 켰다"는 무게가 없다.

## 1. 소유 파일 (배타적)

```
src/ui/title.js
src/ui/paper.js  type.js          (타이틀이 쓰는 렌더 유틸 — 필요한 경우만)
assets/                            (신설 시. 지금 저장소에 없다)
```

**남의 파일** — `docs/HANDOFF.md` 등재 후 자기 소유분만. 같은 워킹트리에서 S5(오디오,
`src/audio/`)가 동시에 돌 수 있다.

## 2. 작업 — 두 안을 만들어 비교한다

사용자 지시가 "둘 다 만들어 비교"다. **한 안만 만들고 끝내지 마라.**

### A안 — 인게임 렌더 배경

로비 실제 렌더를 타이틀 배경으로 쓴다. 게임 화면 그대로가 타이틀이 되므로 **"이 화면이 곧 게임"**
이라는 정직함이 있고, 외부 에셋이 0이라 기술 문서의 "전부 절차 생성" 서사가 유지된다.

고려할 것: 어느 앵글인가(데스크 코너가 이 게임 최고 프레임이다 — fable 1차 판정) ·
느린 카메라 드리프트를 줄지 · 어둡게 깔고 비네트·그레인을 얹을지 · 타이틀 텍스트와의 대비 확보 ·
**부팅 비용**(타이틀에서 이미 로비를 렌더하면 첫 로딩이 늘어난다 — 실측해라).

### B안 — AI 생성 이미지 배경

**요강 확인 완료: AI 생성 에셋은 허용·권장이다.** "AI 도구 사용 허용·권장이나 도구·활용 내역
필수 기재"이며, AI 합성 금지 조항은 **플레이 동영상에만** 걸린다. 외부 에셋은 기술 문서에
출처·라이선스를 명시하면 된다(제출물 4번).

**이미지는 사용자가 ChatGPT 앱에서 생성해 파일로 준다** — codex CLI에는 이미지 생성 기능이 없다
(`-i/--image`는 입력 첨부용). 파일이 아직 없으면:

1. **프롬프트 초안을 네가 써서 사용자에게 제시해라.** 1947년 LA, 아르데코 호텔 파사드 또는
   로비, 느와르 톤, 앰버/세피아, 마리오네트 아트 디렉션과 충돌하지 않을 것 — 인물은 넣지 마라
   (인게임 인형 스타일과 어긋난다).
2. 파일이 오면 결박한다. **자체 검증 필수**: 용량(전체 번들 3.5MB 기준 — 배경 1장이 이를 크게
   넘기면 안 된다) · 반응형(16:9 외 비율에서 잘림) · 로딩 순서(이미지 전에 텍스트가 뜨는 깜빡임) ·
   `--base=./` 빌드에서 경로가 깨지지 않는지.

### 공통 — 두 안 모두 지켜야 할 것

- **웹 UI 냄새 금지**(실격 D7). 시스템 폰트·기본 버튼·라운드 코너·이모지 0. 현재 놋쇠 명패
  버튼은 이 기준을 통과한 자산이니 함부로 버리지 마라.
- 저사양 경로(`?q=low`)에서도 성립해야 한다.
- 타이틀 → 벨 클릭 → 인트로 전이가 지금처럼 매끄러워야 한다. **벨 클릭이 브라우저 오디오
  제스처 게이트다**(E8 §3) — 이 경로를 깨면 소리가 아예 안 난다.

## 3. 검증

```bash
SHOT_PORT=5660 node tools/shoot.mjs --out shots/s6 ui-title ui-loading ui-resume
```

- **PNG를 Read 도구로 직접 봐라.** 코드만 읽고 판정하지 않는다(CLAUDE.md).
- 두 안을 나란히 놓고 비교할 수 있게 파일명을 갈라 찍어라(`ui-title__a` / `ui-title__b`).
- 최종 선택은 **사용자가 한다.** 네가 고르지 말고 둘 다 보여줘라.
- 콘솔 에러·경고 0 · `npm run build` 성공.

## 4. 커밋

**같은 워킹트리에서 다른 세션이 동시에 돈다. git 인덱스는 공유된다** — `git add` 를 쓰지 말고
pathspec 커밋을 써라(신규 파일만 예외적으로 `git add <파일>` 후 pathspec 커밋):

```bash
git commit -m "feat: 타이틀 배경 A안 — 인게임 렌더" -- src/ui/title.js
```

- `index.lock` 충돌은 정상 — 2~3초 뒤 재시도.
- `git stash`·`git checkout .`·`git reset --hard` **금지**(남의 작업을 지운다).
- 커밋 후 `git show --stat HEAD` 로 자기 파일만 들어갔는지 확인.
- **이력 재작성 금지**(제출 요건). 파일당 500줄 상한이 계약 린트로 강제된다.

## 5. 완료 보고

- **두 안의 PNG 경로**와 각 안의 장단점. 판단 근거는 "심사자가 이 화면을 3초 봤을 때"다.
- B안을 만들었다면 **사용된 AI 도구·모델·프롬프트 전문**을 보고에 포함해라 —
  제출물 4번(AI 활용 기술 문서)에 그대로 실린다. 이건 선택이 아니라 요강 요건이다.
